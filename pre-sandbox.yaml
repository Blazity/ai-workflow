preSandbox:
  runOn:
    newTicket: true
    existingPr: true
    mergeConflict: true
  steps:
    - uses: require-title-length
      name: Require Title Length
      onFailure: fail
      timeoutMs: 5000
      with:
        input:
          ticket:
            - title
