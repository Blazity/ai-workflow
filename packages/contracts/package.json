{
  "name": "@shared/contracts",
  "description": "Pure shared data contracts that may define cross-application shapes, constants and their validation, and may not perform runtime work, reach infrastructure or import another workspace package.",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./index.ts",
  "types": "./index.ts",
  "exports": {
    ".": {
      "types": "./index.ts",
      "default": "./index.ts"
    }
  },
  "dependencies": {
    "zod": "catalog:"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --import tsx --test \"*.test.ts\"",
    "test:zod4": "node --import tsx --import ./zod4-alias.mjs --test \"*.test.ts\""
  },
  "devDependencies": {
    "zod4": "catalog:"
  }
}
