
## chat package API (v4.20.2)
- `ChatConfig` requires both `state: StateAdapter` and `userName: string` as mandatory fields. There is no built-in no-op state adapter exported; you must implement the `StateAdapter` interface yourself for outbound-only use cases.
- `createSlackAdapter` accepts `SlackAdapterConfig` — the bot token field is `botToken`, not `token`.
- `StartOptions` in `workflow/api` has no `id` property; valid options are `deploymentId`, `world`, and `specVersion`.

## Sandbox push & branch creation
- `@vercel/sandbox` git clones can be shallow by default, causing "no history in common with main" on PR creation when force-pushing from the sandbox. Always unshallow before pushing (`git fetch --unshallow origin`).
- `GitHubAdapter.createBranch` must force-reset existing branches to the base SHA on 422, not silently return. Stale branches from previous failed runs can retain orphan history.

## Overview KPIs / Workflow run store
- The Vercel Workflow `/v2/runs` list API (`world.runs.list({ pagination: { limit } })`) **caps `limit` at 100** — `limit > 100` returns HTTP 400 Bad Request, which surfaces as a thrown `WorkflowWorldError`. `collectKpis` defaulted to `limit: 500`, so the KPIs endpoint always threw → caught → `runs24h:null` → dashboard showed "N/A", while `/api/v1/runs` worked because `collectRuns` uses `limit: 50`. Keep any `world.runs.list` limit ≤ 100. Verified empirically 2026-06-03 (50/100 OK, 200/500 → 400).
- The dashboard reads KPIs from the **deployed** worker (`WORKER_BASE_URL`), so worker-side fixes don't show on localhost until redeploy. A *local* worker can't validate run-store data either (dev-scoped OIDC → 0 runs, see [[project_workflow_run_history_auth]]). `apps/dashboard/lib/api/derive-kpis.ts` derives `runs24h`/`p95`/`errors24h` from the runs list as a per-field fallback when the worker returns null, so the tiles populate on localhost without a deploy.

## Human-in-the-loop ("Input needed")
- Clarifications are NOT a durable workflow pause. When a phase returns `needs_clarification`, `agent.ts` unregisters the run, posts the questions as a Jira comment, moves the ticket to `COLUMN_BACKLOG`, and ends. The human answers on Jira and re-queues to `COLUMN_AI`; the cron re-picks it as a fresh run. The bot's ONLY Jira comment path is `postClarificationAndMoveBack` (failures notify Slack only, post no comment) — so "ticket in backlog + latest comment authored by the bot" is a reliable awaiting-input signal. Surfaced via the `needs-clarification` label (added on ask, removed on re-pickup to avoid a stale label resurfacing failed tickets) + `collect-awaiting-runs.ts` scanning that label.

## Jira adapter
- Jira REST v3 comments require ADF, and **ADF text nodes cannot contain `\n`**. Multi-line content must be modeled as multiple paragraph nodes (or use `hardBreak` inline nodes between text nodes). Stuffing newline-joined text into a single text node returns 400 Bad Request on `/rest/api/3/issue/{id}/comment`. Adapter helper `toAdfParagraphs` splits on `\n` and emits one paragraph per line.

## Codex agent in Vercel Sandbox
- The `using-git-worktrees` superpowers skill is the dominant root cause of empty-PR / `.gitignore`-only commits, NOT `.codex/` pollution. The skill's contract: "If the worktree directory is not in .gitignore, add it and commit before proceeding." The `executing-plans` skill REQUIRES `using-git-worktrees`, so any prompt that tells the agent to use `executing-plans` chains into "modify .gitignore + commit" before any real implementation work. Confirmed on AWT-641 / AWT-642. Mitigation: prompts (research + implement) explicitly forbid invoking `using-git-worktrees`/`executing-plans` and forbid any `git worktree` command or `.gitignore` change. The block is in the prompt body and must override conflicting skill text.
- Codex CLI also creates `.codex/` in cwd at runtime (per-session state). Without intervention, the agent sees it as untracked pollution in `git status`, "fixes" it by adding `.codex/` to `.gitignore`, commits only that. Mitigation: `CodexAgentAdapter.configure` writes `.codex/` to `.git/info/exclude` so it's hidden from the agent's git status. The post-phase cleanup `rm -rf .codex/` exists for the same reason but only runs after the agent exits.
- `extractUsage` cannot derive duration from Codex NDJSON event timestamps (the events do not carry `timestamp`/`ts`/`time` keys). The wrapper script appends a synthetic `{"type":"phase.duration","duration_ms":N}` line to stdout as a fallback so Slack reports show real wall-clock minutes instead of `0m`.
- Codex Stop-hook protocol accepts BOTH `{"decision":"block","reason":"..."}` (legacy) and `{"continue":true,"stopReason":"..."}` (new) on stdout with exit 0 to force the agent to take another turn — confirmed against developers.openai.com/codex/hooks. Either format works; the codebase uses the legacy one for parity with the Claude commit-guard.
- `fixAndRetryPush` must dispatch the configured agent's CLI, not hardcode `claude`. When AGENT_KIND=codex the claude binary isn't installed and `|| true` swallows the failure, leaving the same broken HEAD to be force-pushed.

## Claude Code agent auth in sandbox
- The Claude Code CLI rejects OAuth tokens (`sk-ant-oat...`, issued by `claude setup-token`) when supplied via `ANTHROPIC_API_KEY` — that variable only accepts standard API keys (`sk-ant-api...`). OAuth tokens must be exported as `CLAUDE_CODE_OAUTH_TOKEN`. Commit `350a754` ("drop CLAUDE_CODE_OAUTH_TOKEN, require ANTHROPIC_API_KEY for Claude") consolidated to a single operator-facing var but lost the OAuth path; agent runs failed with "Invalid API key" at the research phase. Fix in `src/sandbox/agents/claude.ts:configure` — detect `sk-ant-oat` prefix and route the value to `CLAUDE_CODE_OAUTH_TOKEN` inside the sandbox, so the operator still pastes into a single `ANTHROPIC_API_KEY` env var.

## E2E in GitHub Actions
- `@vercel/sandbox` reads credentials from `process.env` — a GH secret is not enough; it must be mapped via the job's `env:` block (e.g. `VERCEL_OIDC_TOKEN: ${{ secrets.VERCEL_OIDC_TOKEN }}`). Prefer long-lived `VERCEL_TOKEN` + `VERCEL_TEAM_ID` + `VERCEL_PROJECT_ID` over OIDC — OIDC tokens expire in ~12h and the SDK's refresh path requires `.vercel/project.json`, which CI doesn't have.
- Reconcile (`src/lib/reconcile.ts`) has a 30s `ORPHAN_GRACE_MS` window that skips entries younger than 30s. Any e2e test seeding a registry entry via `setEntry` and expecting reconcile to cancel it on the next cron tick must backdate the timestamp past the grace window (`setEntry(key, runId, { ageMs: 60_000 })`). Without backdating the test is racy — it only passes if Vercel's 1-min scheduled cron happens to fire at T>30s during the test's wait window.

## collectRuns has two callers (collectWorkflows shares its options)
- `CollectRunsOptions` is reused by `collectWorkflows` (`export type CollectWorkflowsOptions = CollectRunsOptions`), which delegates to `collectRuns`. So adding a *required* field to `CollectRunsOptions` breaks BOTH `routes/api/v1/runs.get.ts` AND `routes/api/v1/workflows.get.ts` (+ `collect-workflows.test.ts`). When threading a new option through `collectRuns`, update all of them. Discovered 2026-06-03 implementing run-id ticket labels (plan only listed the runs.get.ts caller).

## Per-run trace data: what Vercel Workflow exposes (vs. what's mock)
- `getWorld()` exposes `runs`, `steps`, `events`, `hooks`. For a single run trace:
  `world.runs.get(runId, {resolveData:'none'})` → status/workflowName/timestamps/error/deploymentId, and `world.steps.list({runId, resolveData:'none'})` → flat (no parent) step list: stepId/stepName/status/attempt/timestamps/error. This backs `/trace/[runId]` (`collect-run-detail.ts` → `GET /api/v1/runs/[runId]`).
- Use `resolveData:'none'` for steps too (same expired-run schema-crash reason as `collect-runs.ts`). Step `input`/`output` are encrypted at rest and `hydrateResourceIO` does NOT decrypt → the trace shows step I/O as "encrypted, not viewable". Parse step labels with `parseStepName(name).functionName`.
- NOT available per run (all were mock in the old `/trace` screen, now dropped): tokens/cost (computed in-workflow as `phaseUsages`, emitted only to Slack — never persisted to a queryable store), Arthur eval scores (separate system), sandbox test results, PR diff. Steps have no "kind" (llm/tool/guard) — the trace colors by status instead.
- Ticket for a run is recovered the same way as `collect-runs`: JQL `labels in ("run:<id>")` (the dispatcher's run-label tag), since the workflow `input` is encrypted.
- Removed the standalone `/trace` tab + its `activeRun` cockpit-context state; runs now deep-link to `/trace/${run.id}`. Discovered/implemented 2026-06-03.

## 2026-06-05 — iOS Safari canvas drag hijacked by page scroll
In flow-editor.tsx (FlowCanvas), touch drag/pan on iOS jumped ~5px then stopped, and only worked once the page was scrolled to the bottom. Root cause: iOS Safari treats a touch-drag as a page scroll whenever the scrollable ancestor still has room, firing pointercancel and killing our pointer gesture. `touch-action: none` on the element AND React's onTouchMove are NOT reliable here (onTouchMove is passive on iOS). Fix: attach a non-passive native `touchmove` listener with e.preventDefault() on the canvas container (same pattern already used for wheel/pinch). The delta-based drag math was never the problem.

## 2026-06-05 — CORRECTION: real root cause was onPointerLeave, not page-scroll
On-device HUD instrumentation proved the actual cause of the iOS canvas drag bug: the canvas had `onPointerLeave={onPointerUp}`, and iOS Safari spuriously fires `pointerleave` mid-gesture (finger still down and inside the element, even with pointer capture set). That ended the drag one move in — node/canvas jumped ~5px then froze while the finger kept moving. Fix: gate it to non-touch only `onPointerLeave={(e)=>{ if(e.pointerType!=="touch") onPointerUp(e); }}` — pointer capture guarantees a real pointerup/cancel, so pointerleave is only needed as the desktop mouse-left-window fallback. The non-passive touchmove preventDefault was kept (prevents page-scroll hijack / pointercancel) but was NOT the primary fix. Lesson: when reasoning about WebKit event quirks fails twice, add an on-screen HUD logging the raw pointer event stream instead of guessing.

## Arthur GenAI Engine — eval read path (for /evals page)
- Arthur is integrated **write-only** in the worker today: `arthur-tracer.ts` ships OpenInference traces via `POST /api/v1/traces`; `arthur-client.ts` only does tasks/prompts (`/api/v2/tasks*`, `/api/v1/tasks/{id}/prompts*`). Nothing reads evals back.
- Arthur DOES expose a read API (same `Bearer GENAI_ENGINE_API_KEY`, needs `INFERENCE_READ`, org-scoped → matches single-tenant). Primary for fleet eval health: `POST /api/v1/traces/overview` { task_ids, start_time, end_time } → overviews of { trace_count, eval_count, continuous_eval_success_rate, ... }. Per-metric detail: span `metric_results` (metric_type enum is ONLY QueryRelevance|ResponseRelevance|ToolSelection; `details` is opaque JSON, no flat score/pass-fail — parse + threshold yourself). Timeseries: `POST /api/v1/traces/overview/timeseries` (single task per call).
- CRITICAL: the rich rule families the mock shows (hallucination/PII/toxicity/prompt-injection) come ONLY from the legacy `/validate_prompt` + `/validate_response` write path, which we never call. `GET /api/v2/inferences/query` is empty for us. Our trace path yields only success-rate + eval/trace counts + the 3 relevance/tool metric types (and only if continuous evals are configured on the task; else eval_count=0).

## 2026-06-09 — CORRECTION: the Arthur read API above was WRONG; verified against live instance
The /cost + /evals collectors were first built against assumed endpoints that DO NOT EXIST. Verified the real API against the deployed Arthur (`accomplished-beauty-production-8c60.up.railway.app`, FastAPI — fetch `/openapi.json` for ground truth). Hard-won facts:
- **No aggregate/overview endpoint exists.** `POST /api/v1/traces/overview`, `.../overview/timeseries`, and `POST /api/v1/traces/spans` all 404/405. The real shape is row-query + **client-side aggregation**: `GET /api/v1/traces?task_ids=…&start_time=…&end_time=…&page&page_size` returns per-trace `{ task_id, total_token_count, total_token_cost (USD, may be null), start_time, … }`. `GET /api/v1/traces/spans` is GET (not POST) and its rows carry NO `model_name` (model is only in `span_name` for LLM-kind spans, e.g. `"claude/claude-opus-4-6"`) — so "cost by model" was dropped.
- **`task_ids` is REQUIRED** on every trace read (empty → 400 "Field required"). The "empty = all org" assumption was false. Must enumerate tasks first.
- **Pages are 0-INDEXED.** `page=1` skips the first page → empty results (this silently returned $0). Start at `page=0`; loop until `collected >= count` or an empty page.
- **`POST /api/v2/tasks/search` pagination is BROKEN** (page param effectively ignored; unique tasks returned DECREASE as page_size grows: 162→147→122→72; and it omits tasks that actually have traces, e.g. AWP-3.5). Use **`GET /api/v2/tasks?page_size=N`** instead — returns a bare JSON array of all tasks reliably in one oversized page. `ArthurClient.listAllTasks` does this.
- Multi-`task_ids` queries work and accumulate correctly; batch the ids ~50/request to keep the GET URL well under server limits (`ArthurClient.listTraces`/`countTraces` chunk + paginate + merge).
- **Evals: there is no success-rate field.** Compute pass-rate from `countTraces(..., { continuous_eval_run_status })` (enum: `pending|passed|running|failed|skipped|error`): `score = passed / (passed+failed)`. On this instance continuous evals are NOT configured (`GET /api/v2/tasks/{id}/metrics` → 404, trace `metrics` carry only token/cost, `annotations: null`, spans have no `metric_results`), so passed=failed=0 and /evals correctly degrades to `available:false`. The logic lights up once evals are enabled.
- Verified live (MTD): /cost = $26.33 over 94 traces / 23M tokens, real per-workflow + daily breakdown. The dashboard reads the **deployed** worker — these fixes need a worker redeploy to show.

## Pino error logging convention (2026-06-10)
- Pino's error serializer only applies to the `err` key. `logger.warn({ error: err }, ...)` JSON-stringifies the Error to `{}` (message/stack are non-enumerable).
- Codebase convention: `logger.warn({ err: (err as Error).message }, "msg")` — see src/lib/reconcile.ts:145, src/routes/webhooks/github.post.ts:146.
- Known pre-existing violation (out of scope, left as-is): `poll_dispatch_failed` in src/routes/cron/poll.get.ts (~line 94) uses `{ error: err }`.

## 2026-06-10 — Redis→Neon migration implementation learnings
- **Stale `apps/worker/dist/` breaks `pnpm build` after removing a dependency.** The nitro/Workflow-DevKit build scans compiled JS under the gitignored `dist/` tree; a pre-migration `dist/src/adapters/run-registry/upstash.js` still importing `@upstash/redis` failed esbuild resolution after `pnpm remove @upstash/redis`. Fresh clones (Vercel CI) are unaffected. Fix: `rm -rf apps/worker/dist` locally. Also: piping build output through `tail` masks the exit code — check `PIPESTATUS`/run unpiped.
- **`pnpm exec tsx --env-file=X` exits 9 under standalone-pnpm installs** (pnpm's embedded Node makes tsx re-spawn `process.execPath` = the pnpm wrapper, which mishandles the forwarded flag). Root-cause fix used in `scripts/db-migrate.ts` + `scripts/clear-run-registry.ts`: `config({ path: [".env.local", ".env"], quiet: true })` from dotenv 17.x — first file wins, never overrides pre-set process env, so `vercel env pull .env.local && pnpm db:migrate` just works. Note `init-env/SKILL.md:193` still carries the fragile `pnpm tsx --env-file` pattern (pre-existing).
- **`pnpm dedupe` does NOT purge stale optional-peer suffixes** (e.g. `@upstash/redis` baked into `drizzle-orm(...)` snapshot keys when both coexisted) — it churned ~950 lockfile lines without removing the package. Reverted; the stale entry is peer-only, nothing imports it, harmless.

## 2026-06-16 — E2E orchestration fails after Redis→Neon migration: cross-branch registry seeding
- Symptom: 6 tier2 tests fail after the Postgres migration — us08/us10 (`expected 1 to be 0`: a sandbox got dispatched when the worker should have skipped) and us09/us13/us14/us15 (30s timeouts waiting for the worker to write/clear a registry row). Every failing test SEEDS a Postgres row via `e2e/helpers/registry.ts` (raw `neon(e2eEnv.DATABASE_URL)`) and expects the **deployed** worker (`E2E_BASE_URL`) to read/write the SAME row. Every passing test (attachments, gates us22/26/27, ticket-moved us12) never crosses that seed-here/read-there Postgres boundary.
- Root cause: the e2e harness's `DATABASE_URL` and the deployment-under-test point at DIFFERENT Neon branches. The migration introduced **branch-per-environment** isolation (each VERCEL_ENV → its own branch, enforced by the `env_marker` build guard). The migration plan line 18 says "local dev & e2e hit the Neon **development** branch via `vercel env pull`", but `E2E_BASE_URL`/ai-workflow-demo runs on its own (production/preview) branch. So the test writes branch A, the worker reads branch B. Pre-migration this never happened because the registry was ONE shared Upstash Redis instance addressed by URL+token; branch-per-env Postgres broke the implicit "single shared store" assumption.
- Worker code is NOT at fault: `src/lib/adapters.ts` / `step-adapters.ts` unconditionally `new PostgresRunRegistry(getDb())` (no fallback), `dispatch.ts:52/64` still read `isTicketFailed`/`claim`, `reconcile.ts:185` parses staleness from the `claiming:<ts>` runId string (so us14's seeding is correct). Schema/columns match the helper exactly.
- Why the new `check-db.ts` preflight (commit 889621e) doesn't catch it: it only proves DATABASE_URL reaches a MIGRATED db (tables exist), not that it's the SAME branch — its own comment admits "two migrated branches are indistinguishable from here."
- Decisive disambiguation: compare the `ep-…` endpoint hosts (ignore `-pooler` suffix) of the e2e GH-secret `DATABASE_URL` vs the demo deployment's production `DATABASE_URL` (`vercel env pull --environment=production` on the demo project). Differ → cross-branch confirmed. Also verify the demo was redeployed onto the migrated build (else it's still the Redis build = same symptom, different reason).

## 2026-06-16 — RESOLVED: e2e branch identity (continuation of the cross-branch finding above)
- Topology confirmed via read-only `env_marker`/row-count probes: the demo deployment is `ai-workflow-app` running in the **`ai-workflow-demo` custom environment** (git `dev`). The Neon Vercel integration created branch **`ai-workflow-demo/dev`** (host `ep-late-mode-a2yj03oo`, parent `main`) for it — that branch has the live demo history (108 `workflow_runs`, 10 `thread_parents`) and is what the deployment reads/writes.
- The local `apps/worker/.env` (and the stale GitHub `e2e` secret) pointed at a DIFFERENT host (`ep-jolly-hill-a250q4ge`, empty, 0 rows) → split-brain → the 6 failing tests. Fix: set the GitHub `e2e` environment secret `DATABASE_URL` to the `ai-workflow-demo/dev` pooled connection string (`ep-late-mode-a2yj03oo`).
- GOTCHA: a freshly-branched Neon branch carries a COPIED `env_marker` row showing the PARENT's `env`/`endpoint_host` (here `ai-workflow-demo/dev` still showed `env=preview, host=ep-jolly-hill`). So `env_marker` is NOT a reliable branch-identity signal until the next deploy re-claims it — identify a branch by its endpoint host + row data, not its marker. This briefly misled the diagnosis.
- Lesson reaffirmed: two migrated Neon branches are indistinguishable by `check-db.ts` (tables exist on both). Only an endpoint-host comparison or a sentinel round-trip through the deployment proves same-branch.

## 2026-06-16 — E2E can't run locally: Jira token is basic-auth, harness+worker need a Bearer token
- Both the worker (`src/adapters/issue-tracker/jira.ts:27`) and the e2e helper (`e2e/helpers/jira.ts:4`) authenticate to Jira as `Authorization: Bearer ${JIRA_API_TOKEN}` against the `api.atlassian.com/ex/jira/{cloudId}` gateway (cloudId discovered via the unauthenticated `/_edge/tenant_info`). They require an **Atlassian OAuth / scoped Bearer token**.
- The `JIRA_API_TOKEN` in local `.env` / `.env.local` / `.env.e2e` is a **classic Atlassian API token**: it authenticates fine via **Basic** `base64(JIRA_EMAIL:token)` (→ 200, human account `ai-workflow@blazity.com`, locale `pl_PL`) but returns **401 as a Bearer**. So every e2e test that calls `createTestTicket()` fails at `POST /rest/api/3/issue` with `400 {"errors":{"project":"…目标项目不存在，或者您无权…"}}`. The real Bearer token is a *service account* (`ai-workflow-…@serviceaccount.atlassian.com`, locale `zh_CN`) — that's why genuine Jira errors come back in Chinese. The misleading part: a wrong/unauthenticated Bearer still surfaces as a 400 "no permission" rather than a clean 401 on `/issue`, so it reads like a project-permission problem. Probe `/rest/api/3/myself` with the Bearer to disambiguate (401 = wrong token type).
- Net: the real Bearer token lives only in the Vercel deployment env (ai-workflow-demo) and the GitHub `e2e` environment secrets — never in local files. To run locally, pull the demo deployment's env (`vercel env pull` of the ai-workflow-demo environment) and lift its `JIRA_API_TOKEN` (a 192-char Bearer service-account token, `havePermission CREATE_ISSUES=true` in AWT) into `.env.e2e`, replacing (not appending — the harness `.env.e2e` parser is first-wins) the basic-auth token. Note: api.vercel.com is blocked from the agent sandbox, so the operator must run the pull.

## 2026-06-16 — E2E re-run with the correct Bearer token: the 6 registry tests STILL fail = split-brain persists AT RUNTIME
- After swapping in the Bearer token, orchestration = 6 passed / 6 failed. Passing (us02, us12, us22, us26, us27) are exactly the tests that do NOT require the worker and harness to share the `active_runs`/`failed_tickets` tables. The 6 failures are the SAME set as the original cross-branch finding: us08/us10 (`expected 1 to be 0` — worker dispatched despite a seeded skip row) and us09/us13/us14/us15 (30s timeouts — worker never wrote/cleared the seeded registry row).
- The earlier "RESOLVED" entry was PREMATURE. Repointing `.env.e2e`/config at `ep-late-mode` is necessary but NOT sufficient: the *running* deployment is still on a different branch. Proof (read-only probe of `ep-late-mode` right after the run): `workflow_runs` newest row = 07:51 UTC while `now()` = 10:22 UTC, **0 rows in the last 15 min** despite us08/us10/us12 triggering real dispatches minutes earlier; `active_runs`/`failed_tickets` both empty. The worker's live writes are NOT landing on `ep-late-mode`.
- Interpretation: the Neon DATABASE_URL config for ai-workflow-demo is correct (`vercel env pull` returns `ep-late-mode`), but Vercel only applies env changes on a fresh deploy — the running deployment predates the change and still injects the old branch (likely `ep-jolly-hill-a250q4ge`, the empty split-brain host). The historical 108 `workflow_runs` on `ep-late-mode` stop at ~07:51 UTC, consistent with a redeploy switching the live branch around then.
- DECISIVE NEXT STEP (can't be done from the agent sandbox — api.vercel.com blocked): confirm the RUNNING demo deployment's runtime `DATABASE_URL` host (Vercel dashboard → deployment → runtime logs/env, or a deploy-time echo), then **redeploy ai-workflow-demo from current dev** so the corrected `DATABASE_URL` (ep-late-mode) actually takes effect. Re-run orchestration after the redeploy; the 6 should go green.

## 2026-06-16 — Dashboard read path switched from Workflow world API → Postgres
- `/api/v1/runs`, `/overview/kpis`, `/workflows`, `/cost` now source from the `workflow_runs` telemetry table (new module `apps/worker/src/db/queries/runs-read.ts`), NOT `getWorld().runs`. This is what unlocks: per-run cost/tokens (already persisted, the world API has neither), real time-window filtering on `started_at`, and indexed ticket search. `/runs/live` (registry+Jira) and `/overview/eval-health` (Arthur) are unchanged.
- **`workflow_runs.status` stores an ALREADY-MAPPED `RunStatus`** (`success|failed|running|blocked`, or null), because `collect-snapshots.ts:78` writes `STATUS_MAP[run.status]`, not the raw Workflow status. So the read path uses the column value directly (null → "running" for usage-recorded-before-snapshot rows). "awaiting" is NEVER persisted — it's a live-only concept from the registry/Jira path.
- Filtering is parameterized SQL only: `window` is whitelisted to `24h|7d|30d|all` → a JS-computed cutoff `Date` bound as a param; `q` is a bound ILIKE with `\ % _` escaped. The dashboard sends typed intent (URL params), never SQL. Verified against the live `ep-late-mode` branch: window counts scale (24h:39→all:135), `q="' OR 1=1 --"` → 0 matches.
- Trace `/runs/[runId]` keeps the world step-waterfall as primary but falls back to `run-detail-read.ts` (header from the row + a phase waterfall synthesized from the persisted `phases` jsonb) when the world lookup throws (expired ~24h runs).
- The old `collectKpis`/`collectWorkflows` functions are now unused by routes (kept: `collect-runs` exports `STATUS_MAP`/`mapWorkflow`/`RunsLister` for `collect-snapshots`; `collect-workflows` exports `registryRows` for the workflows-route DB-down fallback; `collect-kpis` exports `percentile`). Left in place — not deleted.
- Dashboard: window/search are URL-param driven. Pages (`app/(cockpit)/*`) are now async server components reading `searchParams` (Next 15 → it's a Promise); the Suspense is keyed on `window` only so a window switch shows the skeleton while `q` changes re-render in place (search input keeps focus). The `WindowSelector`/`SearchBox` live in `components/cockpit/controls.tsx`; shared window vocab in `lib/window.ts`.
- GOTCHA reproducing a worker query locally via tsx: importing ANYTHING that transitively imports `env.ts` (e.g. `getWorkflowRegistry`, `getDb`) triggers full t3-env validation at module-eval time, which runs BEFORE a top-of-file `dotenv config()` (ESM hoisting) → "Invalid environment variables". The query functions take `db` as a param, so build a `neon()`/`drizzle()` client directly from `process.env.DATABASE_URL` and inject it — never import `getDb`/`env`-reading modules in a throwaway script.
- The DEPLOYED dashboard reads the DEPLOYED worker (`WORKER_BASE_URL`); these worker changes need an ai-workflow-demo redeploy before the live dashboard shows windowed/searchable Postgres data (operator step).

## 2026-06-16 — RESOLVED: redeploy fixed it, orchestration 12/12 green
- Operator redeployed ai-workflow-demo from current dev (commit 4ed6109). A temporary `CRON_SECRET`-gated `GET /db-identity` route (reports runtime `new URL(env.DATABASE_URL).host` + a `neon()` roundtrip: env_marker, workflow_runs/active_runs counts) confirmed the running worker now connects to `ep-late-mode-a2yj03oo` (workflow_runs=108) — same branch the harness seeds. Re-ran `test:e2e:orchestration` → **12/12 pass** (all 6 previously-failing registry tests green). The endpoint was removed after.
- The fix was purely operational (redeploy to apply the env), NOT a code change. The migration code itself was correct all along.
- env_marker is STILL unreliable as branch identity: `/db-identity` showed `env_marker = {env: preview, endpoint_host: ep-jolly-hill}` while the live connection was `ep-late-mode`. The authoritative runtime signal is `new URL(env.DATABASE_URL).host` (what `getDb()`→`neon()` actually dials) + a row-count probe — NOT the env_marker row.
- The cron concern ("scheduled cron only fires on production main, not on the dev/demo deployment") is real but does NOT affect tier2 e2e: the harness drives cron via an HTTP `GET /cron/poll` (Bearer CRON_SECRET + `x-vercel-protection-bypass`), and us13 uses the Jira-webhook dispatch path — both independent of Vercel's scheduler.
- To run orchestration e2e end to end locally you need TWO non-obvious things beyond a green diff: (1) `.env.e2e` with a Bearer service-account `JIRA_API_TOKEN` (basic-auth token → 401), and (2) the deployment under test actually redeployed onto the branch `.env.e2e`'s `DATABASE_URL` points at.

## 2026-06-16 — Phantom "running" runs: `workflow_runs.status` had a single unreliable writer (the cron)
- Symptom: the dashboard /runs view showed 3 runs "running" with none actually active. They were completed-*successfully* runs (each had a persisted `pr_url`) frozen at "running".
- Root cause: `workflow_runs.status` was written ONLY by the poll-cron snapshot (`collectSnapshots`/`upsertRunSnapshots`, mapping `STATUS_MAP[world.run.status]`). The run's own completion writer `recordRunUsage` (agent.ts outer `finally` → `recordRunTelemetryStep`) recorded cost/tokens/PR but NO status, and `status` has no DB default → null. `coerceStatus` maps null→"running". On the demo/dev deployment the scheduled cron doesn't fire (Vercel runs crons on production main only), so the cron snapshotted each run once while in-flight (status='running', started_at set, completed_at null) and never again post-completion → status frozen at "running". (Even on prod this can bite: the world `/v2/runs` list caps `limit` at 100 and retains ~24h, so a run that completes between snapshots/expires can freeze.) Latent companion bug: phase-failure paths `return` normally (don't throw) → world status "completed" → cron mapped them to "success", so failures showed as successes wherever the cron ran.
- Fix (3 parts):
  1. The workflow writes its OWN terminal status. `agent.ts` tracks `let runOutcome: "success" | "failed" = "failed"`, flips to "success" only on the PR-opened success path + the 3 clarification exits (clarification completes cleanly, matches prior world→success behavior), and threads it into `recordRunUsage` via `recordRunTelemetryStep`. Every phase failure/timeout/throw keeps "failed".
  2. `recordRunUsage` now sets `status` + `completed_at` (`coalesce(existing, now())`) + `duration_sec` (computed in SQL from `now() - coalesce(started_at, created_at)`, ONLY when a start is known — stays null rather than fabricating 0 on a workflow-only row the cron never touched).
  3. `upsertRunSnapshots` got a no-downgrade guard: `status = case when workflow_runs.status in ('success','failed','blocked') then workflow_runs.status else excluded.status end`. The cron may advance a not-yet-terminal row (running→terminal, running→blocked on cancel) but must NEVER revert a workflow-written terminal status — the world reports a failed-but-returned run as completed→success, and there's a brief post-completion window where it still reads "running". "blocked" (external cancellation) stays cron-driven; the workflow never writes it.
- Reliable success signal in the row: `pr_url` is written ONLY by `recordRunUsage`'s success path. The cron writes `pr_repo`/`pr_number` (gate PRs) but NEVER `pr_url`. So `pr_url IS NOT NULL` ⇒ the agent run reached the PR-opened success path.
- Remediated the 3 frozen rows on the demo Neon branch (`ep-late-mode`) with a one-off backfill (`status='running' AND pr_url IS NOT NULL → 'success'`, filling completed_at/duration from `updated_at` ≈ last write). Distribution after: 97 success / 23 blocked / 15 failed / 0 running. Rows that pre-date the fix don't self-heal (recordRunUsage runs once per run); only future runs get the workflow-written status.

## 2026-06-17 — GOTCHA: `posttooluse-validate: workflow` false-positives on dashboard client components
- Editing any `"use client"` React component under `apps/dashboard` (e.g. `components/cockpit/screens/trace.tsx`) trips the workflow-sandbox validator: it flags `setInterval`/`setTimeout` ("use sleep() from \"workflow\"") and `fetch` ("import fetch from \"workflow\"") as errors. These are FALSE POSITIVES — the hook matches because the repo path contains "workflow", but dashboard client components run in the BROWSER, where `setInterval`/`fetch`/`router.refresh()` are correct. Do NOT "fix" them by importing from "workflow" (that's only for Workflow DevKit step files under `apps/worker`). The hook's line numbers are also unreliable (flagged a `fetch` on a line that was `Date.parse`).
- Trace view live-tail (no full reload): `TraceScreen` is a client component fed `data` by the server component `app/trace-data.tsx` (`getJSON('/api/v1/runs/:id')`). The `(cockpit)` layout does no fetching, so a 1s `router.refresh()` re-runs only the trace page's server fetch and updates props while preserving client state (step selection/scroll) with no Suspense skeleton flash. Gated on non-terminal status (running/awaiting) so it stops once the run finishes. No new same-origin proxy route needed (unlike the spotlight's `/api/runs/search`).

## 2026-06-17 — Phantom "running" runs, round 2: the dashboard live overlay overrides the authoritative store
- Symptom (again): /runs showed succeeded tickets as "running". But this is a DIFFERENT root cause than the 2026-06-16 fix. That fix made `workflow_runs.status` authoritative & correct — a live Neon probe confirmed it (`active_runs`=0, `workflow_runs` histogram = 97 success / 23 blocked / 15 failed / 1 null, **0 running**). So the store is right; the staleness is purely a render-layer override.
- Root cause: `/runs` is a MERGE of two feeds in `apps/dashboard/app/runs-data.tsx` → `mergeLiveRuns`. Feed 1 = `/api/v1/runs` (Postgres `workflow_runs`, authoritative). Feed 2 = `/api/v1/runs/live` → `collectLiveRuns` (`lib/overview/collect-live-runs.ts`), which lists the **run registry** (`active_runs` via `PostgresRunRegistry.listAll()`) and HARDCODES `status: "running"` for every entry with no liveness check. The old `mergeLiveRuns` treated live rows as truth: `[...liveRows, ...runs.rows.filter(r => !liveIds.has(r.id))]` — it DROPPED the store row on id collision and kept the live "running". So any `active_runs` row that outlives its run (orphan) masks the recorded success/failed as a phantom "running" — and even hides the correct row.
- Why active_runs entries linger: happy path deletes the entry (`agent.ts:947` before opening the PR) but failure paths are best-effort (`agent.ts` outer catch does `unregisterRun(...).catch(()=>{})`); the only sweeper `reconcileRuns` runs ONLY in the poll cron, which (per 2026-06-16) does NOT fire on the demo/dev deployment — so orphans there are never swept. Aggravator: `reconcile.ts`'s `unreachableStrikes` is a module-level in-memory Map → resets every serverless invocation → `cleanFinishedRun`'s 3-strike unreachable cleanup can never accumulate.
- Fix (dashboard-side, surgical, defense-in-depth — does NOT depend on the unreliable cron): extracted `mergeLiveRuns` to a pure, testable module `apps/dashboard/lib/merge-live-runs.ts` and made the store authoritative once terminal: drop any live row whose run already has `success`/`failed`/`blocked` in the store before merging. Genuinely in-flight (no terminal store row) and `awaiting` rows are preserved. Regression test `apps/dashboard/lib/merge-live-runs.test.ts` (node:test, run via the worker's tsx: `apps/worker/node_modules/.bin/tsx --test ...`; the dashboard has NO test runner of its own).
- NOT fixed (deliberately out of scope, user chose the overlay-only fix): the registry leak itself — orphaned `active_runs` rows on deployments where the cron doesn't fire, and the `unreachableStrikes` per-invocation reset. The overlay fix hides terminal runs from the phantom but a true zombie whose store status is still null/running would remain.
- Reusable read-only Neon probe (the prod query is blocked by the auto-mode classifier until you approve it naming the prod target): `node --input-type=module` reading `DATABASE_URL` from `apps/worker/.env` directly (shell `$()` interpolation chokes on the URL's `&`), importing `neon` from `apps/worker/node_modules/@neondatabase/serverless`, comparing `active_runs` vs `workflow_runs.status`.
- CORRECTION (same day): the overlay was NOT the cause of the run the user actually pointed at (`wrun_01KVA51TPAFZQ6RN6EBR8S47WY` / AWT-738). That run was NOT in `active_runs`; its `workflow_runs.status` was simply `null` (→`coerceStatus`→"running") even though `pr_url`/cost/phases were recorded. Root cause = DEPLOYMENT LAG: the status-writing `recordRunUsage` fix landed in commit `fa3911e`, but the demo is deployed at `4ed6109` (3 commits behind — `git show 4ed6109:.../run-telemetry.ts` has NO `status:`/`completedAt` in `recordRunUsage`). So the old deployed writer records cost/PR but never status; the demo cron doesn't fire to backfill it → every completed run lands `status=null` and renders "running" in BOTH /runs and recent-runs (same Postgres source). The real fix was already in the repo before the session — it just needs DEPLOYING. The `mergeLiveRuns` overlay fix is a real but SEPARATE hardening; it does not touch null-status store rows.
- Remediation: backfilled the 1 stale row (`update workflow_runs set status='success', completed_at=coalesce(completed_at,updated_at) where status is null and pr_url is not null` — `pr_url` set ⇒ PR-opened success path). Histogram after: 98 success / 23 blocked / 15 failed / 0 null. Pending operator action: redeploy the demo from current `dev` (HEAD `9d93acf`) so future runs write status; until then new completions keep landing null. Lesson: when a "fixed" bug recurs, check the DEPLOYED commit (`git show <deployed>:file`) before re-diagnosing the code — the row's written/null column fingerprint tells you which writer/version touched it.

## 2026-06-17 — Spotlight (⌘K) Escape didn't dismiss: modal exit was bound to input focus
- Symptom: on the spotlight-style ticket search (`apps/dashboard/components/cockpit/spotlight-search.tsx`), Esc didn't close the overlay.
- Root cause: Escape was handled in the search `<input>`'s React `onKeyDown` (alongside Arrow/Enter), so it only fired while that input was the focused element. The overlay auto-focuses the input via a single `requestAnimationFrame(() => inputRef.current?.focus())` on open — if focus wasn't in the input when Esc was pressed (rAF hadn't landed, opened-but-not-typed, or focus drifted), the keystroke never reached the handler. By contrast ⌘K open is handled on `window`, which is why opening always worked but dismissing didn't.
- Fix: handle Escape on a `window` keydown listener registered inside the existing open-scoped effect (the one that locks body scroll + focuses the input), guarded by `open` and torn down on close. Removed the now-redundant Escape branch from the input's `onKeyDown` (kept Arrow/Enter there). Lesson: a modal's dismiss key must be focus-independent (window/document level), never scoped to one inner element's focus.

## 2026-06-17 — Removed `run:<id>` ticket labels (made redundant by the Postgres telemetry table)
- The dispatcher used to tag each Jira ticket with a `run:<id>` label (add-only, one per run) as the durable ticket↔run mapping, because the Workflow `input` is encrypted and couldn't be decoded back to a ticket. After the Neon migration, `workflow_runs.ticket_key` is the authoritative mapping, populated by TWO label-independent writers: `recordRunUsage` (agent.ts → run-telemetry.ts, writes ticketKey/title from the run's own knowledge on completion) and `collectSnapshots` (cron, reads ticketKey from the `active_runs` registry — explicitly makes NO Jira calls). So the label was no longer load-bearing.
- The label consumers were either DEAD or REDUNDANT: (1) `collectRuns`→`buildRunLabelMap` (JQL `labels in ("run:<id>")`) was reachable only via `collectWorkflows`, which no route calls anymore (the `/runs` + `/workflows` routes read Postgres via `runs-read.ts`); (2) `collectRunDetail`→`resolveRunTicket` was the live trace's PRIMARY ticket source but the `[runId].get.ts` route already backfilled ticket/PR from `fetchRunRefs` (Postgres). Removed: the producer (`dispatch.ts` updateLabels block + orphaned `errorMessage`), `labels.ts` `RUN_LABEL_PREFIX`/`runLabel` (kept `NEEDS_CLARIFICATION_LABEL`), the dead `collectRuns`/`collectWorkflows` functions (kept live exports `STATUS_MAP`/`mapWorkflow`/`RunsLister`/`WorkflowRunRecord`/`registryRows`), `resolveRunTicket`+JQL helpers, and 3 now-empty test files. `collectRunDetail` no longer takes `issueTracker`/`jiraBaseUrl`/`projectKey`; it returns an empty ticket and the route fills it from Postgres unconditionally.
- KNOWN minor consequence (accepted): an in-flight agent run, before `recordRunUsage` fires, on a deployment where the scheduled cron does NOT fire (demo/dev), has no `workflow_runs` row yet → its trace header shows no ticket until completion. The `active_runs` registry still has the mapping; closeable later by having the trace route read `active_runs`. Not done — the user scoped this to producer+consumer removal only.
- NOT removed: nothing writes run ids into Jira anymore, so the human/operator "which runs touched this ticket" breadcrumb in Jira is gone. The user confirmed it wasn't needed.

## 2026-06-18 — Cron-less agent runs showed as `wf_unknown` and counted under no workflow (recordRunUsage didn't record workflow identity)
- Symptom: Overview "Recent runs" listed N agent runs, but the "Workflows" table showed `runs24h=0` for every workflow, and runs read as `wf_unknown` / "—". (Also `p95=0`.)
- Root cause: `workflow_runs.workflow_id`/`workflow_name` were treated as "cron-owned" — set only by `collectSnapshots`/`upsertRunSnapshots` from the Workflow world. The agent's own completion writer `recordRunUsage` (agent.ts → run-telemetry.ts) did NOT set them. On the demo deployment the poll cron wasn't firing, so recent runs landed in `workflow_runs` only via `recordRunUsage` → `workflow_id` NULL. `runs-read.ts` `mapRun` renders `workflowId ?? "wf_unknown"`; `workflowAgg` buckets by `workflowId === w.id`, so NULL-id runs vanish from the workflows table. (Same null-start cause for `p95=0`: no `startedAt` → no duration.)
- Fix: added `workflowId`/`workflowName` to `RunUsage` and persisted them in `recordRunUsage` (insert + on-conflict `excluded.*`); `agent.ts` passes its static identity `wf_agent` / "Agent". This completes the same "make it workflow-owned because the cron may not fire" migration already done for status/model/ticket*. RED→GREEN test in run-telemetry.test.ts; 525/525 worker tests pass; typecheck clean. NOT yet committed/deployed — only deploy fixes FUTURE runs.
- One-off backfill (existing rows are not retroactively fixed by the code): `UPDATE workflow_runs SET workflow_id='wf_agent', workflow_name='Agent' WHERE workflow_id IS NULL` on the **ai-workflow-demo** Neon branch. Safe because only `recordRunUsage` (agent-only) ever inserts a NULL `workflow_id`; the cron always sets it. 4 rows fixed; live `/runs` + `/workflows` then agreed (wf_agent runs24h=3). The demo env DATABASE_URL is on the `ai-workflow-demo` custom Vercel environment (`vercel env pull --environment=ai-workflow-demo`), a different branch from local `.env`.
- Operational (separate, deprioritized by user): the ai-workflow-demo poll cron (`/cron/poll`, `* * * * *`) appears not to be firing reliably — no new runs in ~19h and recent runs had null `durationSec` (never snapshotted mid-flight). That's the upstream reason runs were cron-less. Vercel only runs crons on production deployments; the demo is a custom env — likely why.

## 2026-06-18 — Test harness replays migrations from DISK, not git (commit not required for column-dependent tests)
- `apps/worker/src/db/test-db.ts` `createTestDb()` does `readdirSync`/`readFileSync` over the `drizzle/` directory and replays every `*.sql` into PGlite. Its own comment says "applies the committed drizzle/ migration SQL", but it reads files from the working tree — an **uncommitted** `drizzle/0003_*.sql` (e.g. fresh from `pnpm db:generate`) is picked up immediately. So a new Drizzle column works in unit tests the moment the migration file exists on disk; you do NOT need to commit it first. (Relevant when implementing under a no-commit constraint, or wondering why a `git`-clean check isn't what gates the tests.) Production is different — `pnpm db:migrate` (build step) applies 0003 to Neon on deploy.

## 2026-06-18 — `agent.ts` workflow has NO top-level adapter/logger imports — all deferred
- `apps/worker/src/workflows/agent.ts` is a Vercel Workflow DevKit `"use workflow"` module. The WDK bundler cannot tolerate top-level adapter/logger imports, so EVERY use of `logger`/adapters inside a `"use step"` is a deferred `await import("../lib/logger.js")` / `await import("../lib/step-adapters.js")` in the step body (logger refs at lines 73,137,201,338,445,486,508 — none top-level). Do NOT assume "a warm module is already imported at the top" — there isn't one; the module is only cache-warm because earlier steps in the same run imported it. (Correction to a wrong assumption I made during the PR-link-comment work: I claimed logger was imported top-level; it is not — an opus reviewer caught it.) Consequence for best-effort steps that log inside a `catch`: that import is itself a deferred `await import` that could in theory fail cold, so a "never throws" step is only truly safe in concert with `maxRetries=0` + a failure mode that doesn't affect the run outcome.

## 2026-06-18 — us11 capacity e2e flakiness = Jira JQL index is non-monotonic, not a product bug
- `e2e/tier2/us11-capacity-limit-respected.test.ts` intermittently failed with `expected 0 to be greater than or equal to 1` on `pollRes.body.discovered`. Root cause: the cron's discovery (`/rest/api/3/search/jql`, `project AND status=COLUMN_AI`) hit Jira's eventually-consistent search index during a window where it reported the AI column **empty** — even though the `isTicketVisibleInJql` barrier had just gotten one positive read. A single positive index read does NOT guarantee the cron's immediately-following search (a separate request) also sees the ticket. The cron is idempotent (next tick discovers it), so this is purely a test-reliability defect. Fix: poll the cron in a bounded `waitFor` (30s) until `discovered >= 1`, **re-saturating the dummy registry entries before each poll** (reconcile's `ORPHAN_GRACE_MS = 30s` would otherwise free a slot mid-loop and let a late poll actually start the ticket), asserting `started === 0` every tick. us08 already tolerates this race by asserting only on the *effect* (no registry entry over a 15s window), not on the `discovered` count — us11 was the only test making a single-shot `discovered` assertion.

## 2026-06-18 — Dashboard ticket grid blowout = bare `1fr` honors content min-content
- `apps/dashboard/app/(cockpit)/ticket/[ticketKey]/page.tsx` desktop split-view used `gridTemplateColumns: "280px 1fr"`. A bare `1fr` is `minmax(auto, 1fr)`, so the detail (trace) column honored its content's min-content (~1082px, driven by the 5-up `grid-cols-5` KPI row + the `lg:grid-cols-[1.4fr_1fr]` panel), pushing the grid to 1410px inside a ~1060px area → horizontal scrollbar inside `cockpit-shell`'s `flex-1 overflow-auto` scroller (`html` itself didn't overflow because the root is `overflow-hidden`). Fix: `280px minmax(0, 1fr)` on the column + `min-w-0` on the `DetailArea` grid item (`ticket-selection.tsx`) so the track + item can shrink below content. Verified live with agent-browser (grid 1410→1060, scroller H-overflow gone). The FlameGraph's own `overflow-x-auto` (fixed 1040px timeline) is intended internal scroll, not a blowout. General rule here: any `1fr` grid track holding wide/min-content-heavy content should be `minmax(0, 1fr)` and its grid item `min-w-0`.

## 2026-06-18 — Prod runs stuck "running", never reaching history = prod Neon branch missing migrations (`steps` column); branch claimed by `preview`
- Symptom: on the PRODUCTION dashboard a run showed only as "running"; after it succeeded it never appeared in history. Worked on dev and demo.
- Mechanism: hybrid trace read (65d5453 "store traces in pg", landed 2026-06-18) — in-flight runs render from the Workflow `world`; finished runs' terminal state is written by `recordRunUsage` (run-telemetry.ts) in ONE `INSERT…ON CONFLICT` that includes the new `steps` jsonb column (migration 0003). The prod branch was missing 0003 (and 0002), so the whole upsert threw; its caller `recordRunTelemetryStep` (agent.ts) is `maxRetries=0` and swallows errors ("telemetry must never fail the run") → terminal status/cost/steps silently lost, row frozen at the cron's last "running" snapshot. The cron writer `upsertRunSnapshots` never touches `steps`, so the live "running" view kept working; the list/history query (`runs-read.ts`) doesn't select `steps` either → no hard error, just no terminal rows to show.
- Root-of-root: migrations only apply at BUILD time (`pnpm db:migrate` → `drizzle-kit migrate` against the per-env injected DATABASE_URL). The prod branch (`ep-jolly-hill-a250q4ge`) was stuck at migration 0001 and its `env_marker.env = "preview"` — prod is reading a branch claimed by the PREVIEW env. Prod reached new code via promote-without-rebuild (or a build that FATALed on the env_marker guard), so its schema never self-healed. Branch-per-environment (SETUP.md §4) is NOT actually isolating prod from preview.
- Fix applied: ran bare `pnpm exec drizzle-kit migrate` (NOT full `pnpm db:migrate` — that also runs the env_marker guard, which with no VERCEL_ENV would re-claim the branch as "development" and break the next prod build) against the prod DATABASE_URL → applied 0002+0003; `steps` present, 4 migrations recorded.
- Aftermath (verified post-fix): STATUS self-heals — the prod cron advances a non-terminal row to the world's terminal status on its next poll, so there were 0 persistent "running" rows (10 rows: 6 success / 4 blocked). What is PERMANENTLY lost is the per-run telemetry `recordRunUsage` owns: every prod row has `cost_usd`/`model`/`steps`/`phases`/`ticket_key` NULL/empty, because that write failed and the cron only writes status+timing. Not recoverable; only FUTURE runs get full telemetry now that `steps` exists.
- Still open: (1) prod/preview INTENTIONALLY share one Neon branch (user confirmed it's fine) — so the env_marker guard FATALs any production *build*, meaning migrations only reach the branch via preview builds; the shared-branch setup means schema won't self-heal on a prod-only deploy and the next migration can break prod again unless a migrate runs against the branch on deploy (or manually). (2) ensure prod actually BUILDS (not just promotes), or run the migrate manually each schema change.

## 2026-06-22 — RECURRENCE on the DEMO branch: same missing-`steps`-migration bug (only prod was fixed on 2026-06-18)
- Symptom (reported as three things, one cause): (a) the `recordRunTelemetryStep` "Finalize" step shows **failed** in Workflow observability; (b) completed runs **don't persist to history (db)**; (c) the trace/live view shows nothing. All three are the SAME root cause as the 2026-06-18 prod entry above — the demo branch `ep-late-mode-a2yj03oo` (`ai-workflow-demo/dev`) was stuck at migration **0001** (last applied 2026-06-15), missing **0002 + 0003**, so the `steps` column didn't exist. The 2026-06-18 fix only touched PROD (`ep-jolly-hill`); the demo branch was never migrated.
- Direct evidence (read-only probe of `ep-late-mode` via local `apps/worker/.env` DATABASE_URL): `information_schema` had no `steps` column, `cost_usd` was still `real` (0002 widens it to `numeric(19,4)`), `drizzle.__drizzle_migrations` had only 2 rows, and **no `workflow_runs` row had persisted since 2026-06-17** (~5 days of lost history). `recordRunUsage`'s single `INSERT…ON CONFLICT` references `steps` → throws `column "steps" does not exist` → the `maxRetries=0` step records failed, the caller `.catch(()=>{})` swallows it so the run completes but no terminal row lands. The dashboard trace-detail read (`fetchRunDetailFromDb`, `db.select().from(workflowRuns)`) ALSO threw because Drizzle emits an explicit column list incl. `steps` — so the missing column broke reads too (the list query `runs-read.ts` still doesn't select `steps`, so it only showed stale rows).
- Fix: `cd apps/worker && pnpm exec drizzle-kit migrate` (bare, not `pnpm db:migrate` — avoids the env_marker guard re-claiming the branch as `development`; local `.env` DATABASE_URL = `ep-late-mode`). Applied 0002+0003; verified `steps` present, 4 migrations recorded, `cost_usd` now `numeric`, and a `select … steps …` no longer throws. The ~5 days of completed runs are unrecoverable (`recordRunUsage` runs once per run; only future runs get telemetry). The demo branch's `env_marker` is still the stale copied parent value (`env=preview, host=ep-jolly-hill`) — bare migrate doesn't touch it, and env_marker remains unreliable as branch identity.
- Hardening (this session): `agent.ts` telemetry call site changed from `.catch(()=>{})` to `.catch((err)=>console.error(...))` — still non-fatal/`maxRetries=0`, but a future schema drift now surfaces in logs immediately instead of silently dropping run history for days. `console.error` in workflow scope is fine (already used in the outer catch).
- LESSON (reinforced): when a "fixed" migration-drift bug recurs, it's almost always a DIFFERENT Neon branch that the earlier fix didn't reach. Migrations apply per-branch at BUILD time only; demo/dev/prod each need their own migrate (or a real build). Check the branch's `__drizzle_migrations` count + the actual column before re-diagnosing the code.

## 2026-06-23 — `db/client.ts` import triggers full env validation (Better Auth dashboard-auth work)
- Discovery: `apps/worker/src/db/client.ts` has a TOP-LEVEL `import { env } from "../../env.js"`. So importing `client.js` AT ALL (even a dynamic `await import(...)`) runs the module initializer → t3-env validates the FULL worker env (Jira/VCS/Slack/etc.) at import time, not lazily. `getDb()` itself is lazy, but the module import is not.
- Why it matters: any standalone/build-time script that wants to run with only a SUBSET of env (e.g. the new `scripts/seed-auth-user.ts`, which warn-and-skips when auth env is missing) must NOT import `client.js`/`getDb()` — it would throw on the full validation and defeat the skip. Build a driver inline (`drizzle({ client: neon(DATABASE_URL), schema })`) and type it via a cast (`as unknown as Parameters<typeof createAuth>[0]`, mirroring `db/test-db.ts`'s `as unknown as Db`). The 2026-06-23 plan's seeder snippet had a dead `await import("../src/db/client.js"); void type;` line that re-introduced this exact hazard; it was dropped during implementation.
- Corollary for tests: to unit-test the `/api/v1` middleware (which imports the env-bound `auth-instance.ts`) without full env, `vi.mock("../auth-instance.js", () => ({ auth: { api: { getSession: async () => null } } }))`. The pure factory `auth.ts` (type-only import of `Db`) is env-free and importable directly.
