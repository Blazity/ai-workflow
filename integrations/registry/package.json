{
  "name": "@integrations/registry",
  "description": "The set of integrations this build ships. Generated by pnpm run gen:integrations from the directories under integrations/, never edited by hand. The root entry is plain manifest data, safe for a browser and for the Workflow DevKit flow bundle; ./worker carries the server runtimes and nothing that reaches a browser may import it. It is the only place core may import an integration from.",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./index.ts",
  "types": "./index.ts",
  "exports": {
    ".": {
      "types": "./index.ts",
      "default": "./index.ts"
    },
    "./worker": {
      "types": "./worker.ts",
      "default": "./worker.ts"
    },
    "./dashboard": {
      "types": "./dashboard.ts",
      "default": "./dashboard.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --experimental-test-module-mocks --import tsx --test \"*.test.ts\"",
    "test:zod4": "node --experimental-test-module-mocks --import tsx --import ../../packages/zod4-alias.mjs --test \"*.test.ts\""
  },
  "dependencies": {
    "@integrations/host-ui": "workspace:*",
    "@integrations/sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "zod4": "catalog:"
  }
}
