{
  "name": "@integrations/sdk",
  "description": "The integration contract: manifest types, the capability ports, the context an integration receives, and the conformance check every integration package passes. Browser-safe and free of Node modules; it may import @shared/contracts and zod only, and re-exports z so integrations never choose their own zod.",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./index.ts",
  "types": "./index.ts",
  "exports": {
    ".": {
      "types": "./index.ts",
      "default": "./index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --import tsx --test \"*.test.ts\"",
    "test:zod4": "node --import tsx --import ../../packages/zod4-alias.mjs --test \"*.test.ts\""
  },
  "dependencies": {
    "@shared/contracts": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "zod4": "catalog:"
  }
}
