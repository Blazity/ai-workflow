Status: draft
Last-verified: 2026-09-18

# The repository record: what a person can do, and what happens in every case

Every subject of work (a ticket, a pull request, a webhook delivery) carries one
durable record of which repositories that work touches. Each entry names a
repository, a state, where the decision came from, who made it, when, and in
which run.

This document is the behaviour the code is measured against. The
[plan](../plans/2026-09-15-repository-work-scope.md) says what is being built
and in what order; this says what a person may do and what they get back. Where
the two disagree, this one is about behaviour and wins on behaviour.

A case marked **held** has a guard in the suite. A case marked **open** is known
and not closed yet. A case marked **deferred** is a decision to do it later, not
an oversight. Nothing here may be marked held on a report; only on a test that
fails when the behaviour is broken.

**As of 2026-09-16 the row-by-row pass has covered the seven rules and all four
sections.** It ties each held row to the test that guards it, and it does not
take an executor's report for one. It was worth running: it found two rows
stating the OPPOSITE of the code (A5 and A7), one row understating what a
refusal costs the person (B4), two cases that were done and still marked in
delivery (A3b and A16), and three behaviours with a guard in the suite and no
row at all, of which one is the way back from an exclusion (B4b, B5b and C10).

**On 2026-09-17 a red team walked the whole user path against this map**, and it
found what a row-by-row pass structurally cannot: the cases nobody wrote a row
for. One row was true in one reading and false in the other and was split (A14
and A14b), and four cases had no row at all (A11b, B10, C10b, and the run that
is asked to enable a repository it cannot reach). The lesson is that a map
audited against the code agrees with the code, so the pass that finds a missing
row has to start from a person doing something rather than from a row.

**Later on 2026-09-17 the reading of a ticket's text was corrected**, because the
round that gave a description its own negation reading gave it too wide a one:
any negation word in the sentence excluded the repositories named in it, so
"Fix the login bug in github:acme/api, but do not deploy yet" dropped api and
told that person their own ticket had said no about it. What keeps this system
out of a repository is now a phrasing about repositories sharing a phrase with
the path (C11u2), the two readers of that text see one segmentation with quotes
handled once (C11u3), and a decline written as a ticket comment is answered on
the ticket (C11s2).

**On 2026-09-18 production found the case a reader of rows could not**, on
AWP-221: a person answered the which-of-these question with one comment holding
the word "no", a line break, and "none of these". Those words were compared with
the refusal list whole, matched no entry in it, and nothing was recorded, so the
comment back told that person to answer "none" the next time the question was
asked, which is the thing they had just written. The same two halves sent as two
comments had always decided (A17), so where somebody pressed enter decided
whether their refusal could be read at all. A reply every phrase of which is a
refusal now declines what the question listed (A17b), and a bare "no" on a ticket
still records nothing (A8). The owner drew the line the same day: a refusal
phrase declines only what it CAN be about, so words about one repository ("no,
continue without it") decline the question that asked about one and are
unreadable under a question listing four (A17c). Four permanent exclusions off a
sentence about one repository is the fabricated decision rule 1 puts below a
repeated question. The same ruling widened the list the other way, in English
only: "neither", "neither of them", "neither of these" and "none of the above"
refuse a set, so each of them declines the whole list and nobody is asked the
same question twice for writing one of them (A17d).

**Later on 2026-09-18 the owner stopped the phrase list rather than extend it
again.** Each of the rounds above added the phrasings that round had met, and the
next person typed a variant nobody had listed. His ruling was that a model has to
decide what somebody meant, and that we do not parse answers people may write,
because we cannot enumerate what they will write. So the reading is now one model
reading into a closed set, made once where the answer arrives and stored beside
the words (A19), not confident means ask rather than guess (A19b), a name the
question did not offer is told back rather than swallowed (A19c), and what that
reading is handed is the person's own words, without the author line the ticket
channel writes in front of each comment (A19d). **Every
phrasing row above is now an EXAMPLE the golden set holds us to, not a list the
code matches.** Adding coverage for a new phrasing means adding a golden row; a
change that adds a phrase to the code is a change going the wrong way.

What a held row still does NOT promise is that the behaviour has been seen in
production. That is the campaign at the end of the plan, and until it has run,
held means guarded by a test, not observed by a person. For the reading in
particular, no golden row is proven by anything on a developer machine: it fails
loudly unless a real model produced the reading, and there is no provider
credential outside the deployment.

## The seven rules every case resolves to

1. **A repeated question is a cost. A fabricated decision is a defect. A run
   that hangs or dies is worse than both.** Whenever a case is ambiguous, pay the
   cost rather than write the defect, and never pay either with the run's life.
2. **Silence is not selection.** Not answering says nothing about anything.
3. **A person decided about a repository only if the question put its name in
   front of them, or they named it themselves.** This governs SUPPRESSION, and
   since 2026-09-17 what a GUESS may take: a repository a question named and the
   answer left out is not taken back by a remembered routing answer, the
   only-accessible shortcut, a discovery proposal or the agent's expansion
   request. It never withholds a repository somebody typed, and the ticket's own
   text is read by WHEN it was written: the description that raised the question
   is the text the answer already spoke about, so it decides nothing afterwards,
   while a full path a person writes in a comment after answering is them naming
   it and attaches (C11f, C11g).
4. **Every recorded decision is reversible in the same delivery.** "Open a new
   ticket" is not a remedy. If a person can be told a decision is final, there
   must be a way to take it back, and they must be told what it is.
5. **A decision is attributed to whoever actually made it, or to nobody.** An
   answer we cannot attribute writes no entry, and the trail still records that
   an answer arrived.
6. **A remedy we offer has to work when the person does it.** Every sentence
   telling somebody what to write is checked against the channel it will be read
   in. Telling a person to reply into a question that is already closed is worse
   than saying nothing, because they do it, nothing happens, and now they also
   distrust the next thing we tell them. What works today: a full repository path
   written in an ordinary ticket comment is read by the next run, because that
   run reads the ticket with its comments, as long as no more than three of the
   repositories it names are still open: more than that and the run asks which
   of them to start from rather than taking any. The comment has to say no about
   nothing: one that refuses a repository is not read as naming any of them, and
   every path in it is reported rather than taken (C11r). What does not: any
   keyword replied after the question has been answered, a path added to a ticket
   that still has more than three open, which the next run asks about instead of
   taking, and a path written beside a refusal in the same comment.
7. **The agent's instruction channel is not the person's channel.** What the
   system PLACES in the agent's prompts (the questions, the memory file's
   "Human decisions" section) is separate from what the agent READS as ticket
   history (the ticket's comments). A lever for reversing somebody's decision
   may appear in the second and never in the first.

## What a person can do

| They want to | They do it by | Reaches |
|---|---|---|
| Answer a repository question | Commenting on the ticket | The record, before the run wakes |
| Answer a repository question | The dashboard's answer screen | The record, before the run wakes |
| Answer a repository question | `runs_answer_clarification` over MCP | The record, before the run wakes |
| See what was decided and why | Reading the record over MCP or the API | The record, unchanged |
| Add a repository to the work | Editing the record (API or MCP) | The record directly, no run needed |
| Take back an exclusion | Editing the record (API or MCP) | The record directly, no run needed |
| Undecide, so the next run asks again | Removing the entry (API or MCP) | The record directly, no run needed |
| Take back leaving a repository out of an answer | Selecting it in the record (API or MCP), or writing its full path in a ticket comment that names only repositories to use, while the ticket names no more than three repositories (C11c, C11r) | The record directly, or the next run's text scan |
| Name a repository without being asked | Writing its full path in a ticket comment that says no about nothing | The next run's text scan |

Two things are deliberately NOT available yet: a screen for the record
(deferred to stage 7, and the owner was told), and any way to tell an agent's
edit from a person's once it is written (deferred, and recorded as a gap below).

## A. Answering a repository question

| # | The person | What must happen | Why | Status |
|---|---|---|---|---|
| A1 | Names one repository that is enabled and reachable | Selected, origin person. The question does not come back | They decided, and we can act on it | held |
| A2 | Names several | All of them selected, origin person | Same | held |
| A3 | Names a repository by a bare or partial name we cannot resolve | Nothing recorded, and the person is told to write the full path in a comment, which the next run reads | Spelled out, that name may well resolve, so the remedy is true for them | held |
| A3b | Names a FULL path this deployment does not hold | Nothing recorded, and the person is told we have no repository by that name, to check it, and that somebody with access to the repositories screen can add it | Telling this person to write the path again is a dead end: the next run's matcher will not resolve it either. Offering a remedy that cannot work is the failure rule 6 exists to prevent, and it is worse than saying less | held |
| A4 | Names one repository we resolve and one we do not, in the same sentence | Their words still reach the next reader, and they are told which name failed | Withholding the whole answer loses the half that was good, and the run then asks the identical question with nothing said | open |
| A5 | Names a repository that exists but is disabled | Recorded as their decision, exactly as if it were enabled. The run refuses it at start, says why, and that reason rides the next question they see. If somebody enables it later, the run attaches it without asking them again | Rule 3 cuts both ways: what a person typed is never dropped. Being enabled is a deployment fact that can change, and their decision outlives it | held |
| A6 | Names a repository that is enabled but the run cannot fetch (renamed, archived, empty default branch) | The selection is honoured as their decision AND the question is not suppressed, because a suppression here strands the work forever | The rule "do not ask somebody who already chose" must read reachability, or the person never gets asked again and nothing is ever attached | held |
| A7 | Refuses and names the subject ("none", "none of these", "żaden") to the which-of-these question | Nothing is written in the record, and an entry an earlier GUESS wrote for a repository the answer left out is removed (C11d). The trail keeps the question, the repositories it NAMED, and the answer naming none of them, and that pair is the whole of the decision: the question does not come back, and no guess takes one of those repositories afterwards (C11). A repository the question counted but never spelled out is bound by nothing. A full path somebody types in the ticket or a comment still attaches (rule 3) | They declined the names they saw, and only those. Writing an entry would record a stronger decision than they made (rule 5) | held |
| A7b | The same words to a question asked because a repository was not enabled, could not be used, or sits outside the workflow's policy | Each repository the question named is written as `unavailable` (the first two) or `excluded` (the third), and that entry governs from then on: an `unavailable` one is taken again once the catalog can use it | A person who could not have the repository has not refused it, and "enable it and start a new run" has to work | held |
| A8 | Refuses without naming the subject ("no", "nie", "nope", "skip it") in a ticket comment, to any question that put repositories in front of them, the which-of-these and the discovery question included | Nothing recorded. The clarification stays answered, the ticket transition is untouched, the run continues, and the person is told what to write. The answer counts nowhere as one: the selection flag, the narrowing flag and the answered set read only a `none` or a `repositories` answer, so the which-of-these question comes back on the next run and no guess is refused on its strength | Nothing threads a ticket comment to our question, so the same word may have been answering the comment above ours. A selection question is no exception: its "no" writes no entry, but it used to settle every repository it named for good through the answered set and the selection flag, signed by that person | held |
| A9 | The same words on the dashboard or over MCP | Recorded as a decline of every repository that question listed, and nothing else. The `runs.answer_clarification` description says so in those words, so a caller knows before it sends one | There the question is on the screen in front of them and the answer is threaded to it, so a bare "no" answers it and nothing else | held |
| A10 | Answers with no words at all ("...", a thumbs up) on the ticket | Nothing is dropped, because a ticket comment always carries its author's name in front of it, so the text is never wordless. The person is told their answer recorded nothing | A reaction is not a sentence, and a thumbs up usually means approval, so silence about it is the worst outcome | held, but its premise is unguarded: no test pins the author prefix on a single comment, so a tidy-up that drops it would turn this row into A10b in production without a red line |
| A10b | The same on the dashboard or over MCP, where the raw reaction is stored | The run proceeds WITHOUT the repositories the question asked about, and the person is told exactly that before anything else, and in the same breath that nothing was recorded about them, so a later run may use them and may ask again. A wordless answer is read as nothing said rather than as "none" (`saysNothingToAttach`), so it never enters the answered set and binds no guess | There the emoji is the whole answer, the run acts on it, and the person has no way of knowing that from the screen | held |
| A11 | Writes prose we cannot read | Nothing recorded, the person is told, the question returns | Better asked twice than recorded wrongly | held |
| A11b | Answers by quoting one of OUR earlier comments and writing underneath it | Our question and every quoted line come out of their words before anything is read, so what is judged is what they typed. A person who quotes the sentence saying a repository was left out and writes "yes, use it" underneath is read as naming it, not as refusing it. What is left outside a quote marker is theirs: a sentence of ours they retyped without one is still read as their own words, and if it carries a no the reply records nothing and they are told. The markers are the ones the channels actually deliver: a leading `>`, Jira's `bq.`, and a Jira quote written in the editor rather than typed, which arrives as a structured blockquote with no marker in it at all and is flattened back to `> ` lines where the reader can see it (round 5, A2) | Every sentence this system writes about a repository it left out is built around the word "not", so reading the whole reply made our own copy refuse the person quoting it, on the reply that is most obviously a yes. Failing towards asking again is the cheap half of rule 1, and reading our own sentence as somebody's no is the expensive half (round 4, M5). The stripping was also dead on the one channel it was built for: Jira sends a comment as a document, the flattener dropped the quote structure, and every quoted line arrived as the person's own words, so the row was true of a plain-text reply and false of the Jira reply it was written for (round 5, A2) | held |
| A11c | Is told what to do next, for a question that listed a repository the catalog does not enable or cannot serve | They are NOT sent to write the path out, because the next run's matcher sees only the repositories it froze at its start and would read nothing. They are told that the catalog cannot serve those repositories, that somebody with access to the repositories screen has to enable them, and that naming one the next time the question is asked records it then | The run itself put that repository in front of them, so a remedy it cannot honour is worse than no remedy: it spends the person's second attempt and teaches them the system ignores them | held |
| A11d | Is told what to do next, for a question asked because a repository sits outside the workflow's policy | They are NOT sent to write the path in a comment. That question is raised mid run and says nothing about how many repositories the ticket names, so nothing on the surface that handles the answer can prove a written path is taken rather than tipping the next run into asking instead. They are told to select it in the work's repository list through the work scope API or the `work_scope.edit` tool, which a policy does not filter, or to name it the next time the question is asked. A path written anyway is read by a run that reads the ticket's text, and that run refuses the repository and says why, naming the limit | Under-offering costs a person one step; offering a route that can end in a question is the dead end rule 6 forbids, and the record route works whatever the ticket says | held |
| A11e | The same question listed one repository a later run can pick up and one it cannot | They get the sentence that is true of the question as a whole: select one in the work's repository list, or name it the next time the question is asked. For the one the catalog cannot serve, a selection is refused at the next run start and that run says why (A5), so the route ends in a spoken refusal rather than in silence, but the sentence does not name the catalog for it | The wider sentence is the one that stays true for somebody, and the comment route is not offered from this surface at all (A11f). This is a residual cost and it is named here rather than discovered later | open, deliberate |
| A11g | Is asked the which-of-these question in the first place | The question says what the answer will bind: a repository it lets them reply with and the answer does not name is left out of this work from now on, and no later run takes it on its own. A repository the ticket names that this work already holds (a path written earlier, a trigger policy, a workflow-owned branch) is never offered as a choice: the question names those as already part of this work and kept whatever the reply, says the reply does not remove them, and binds only the rest. It makes no claim about what else removes them, because each goes its own way (a path when the text stops naming it, a branch when the work no longer owns it). It names no lever, because a question is copied verbatim into the research, implementation and review prompts and into the ticket's memory file, and a lever there is one the AGENT could pull on a person's decision (rule 7). The way back rides the ticket comment beside the question instead | Somebody scoping one afternoon's research cannot be held to a decision nobody told them they were making, and a question offering to drop a repository the answer cannot drop is a false choice | held |
| A11f | Is told what to do next, for a question listing repositories that their answer recorded nothing from | They are never sent to write a path in a comment from here: whether the next run takes a written path is a count of the open repositories the ticket names, and the surface handling the answer has no run behind it and cannot see the ticket. For the question about the ticket's text, which is raised only while more than three open repositories stand, they are told why that route is shut. For every other question, the discovery question included, the sentence says nothing about that route. Both name only routes that work whatever the ticket says: naming one the next time the question is asked, or selecting it in the work's repository list through the work scope API or the `work_scope.edit` tool. The question about the ticket's text is recognised by the opening it shares with the code that asks it (C11q) | Decision logged at joint gate round 3 (R8): a discovery question used to count its own list with the work's answered set and offer the comment within three, and a ticket naming four more open repositories took nothing from the path written. Proving the count here would need a ticket read and the pin on the answer path; under-promising costs a person one step | held |
| A11n | Is told what a refusal can and cannot do from a ticket | They are told that a refusal written under a question already answered records nothing, that answering "none" when the question comes back declines every repository it lists, and that a bare "no" in a comment does not, because nothing ties a comment to the question it was meant for. They are NOT told that nothing written on the ticket can record a refusal, which was false: a comment is how a ticket answers a question that is still open (A7, A17d), and the same delivery posts a comment on the same ticket telling somebody their comment declined what the question listed (C11s2). That sentence also contradicted itself inside one line, sending a person to answer "none" on a ticket it had just called unable to record one | Rule 6: a remedy we offer has to work when the person does it, and one that also contradicts the comment above it teaches them to trust none of what we write | held |
| A11h | Names, in reply to the which-of-these question, a repository it listed as already part of this work | Nothing is recorded about that repository: its entry keeps the reason it is held for, so the way it leaves is unchanged. A choice named beside it is recorded. A reply that names only kept repositories, or says no beside one ("please drop github:acme/web", "github:acme/infra, but not github:acme/web", "drop web", "nie ruszajcie web"), records nothing at all, the repositories the question asked about stay unanswered and come back, and the person is told on the ticket that a reply does not change those, how a kept repository leaves for each reason it can be held, and to name only the repositories to use the next time the question is asked. Which repositories were shown as kept is read from the record at the answer, never out of the question's words, and only for a question that actually listed kept repositories: repository discovery stamps the same ask reason and lists none, and a person answering that one used to be told their reply had been about repositories nobody showed them (round 4, m8) | The question said they stay whatever the reply. Reading a no beside one as a redirect recorded that very repository as the person's own selection and silenced the one they were asked about, with nothing said (joint gate round 3, R1) | held |
| A11i | Says no about anything, in reply to any repository question | The reply records nothing and the question comes back, whatever else it says. A repository named beside the no is not taken, in any phrasing and in any language: "use github:acme/web instead of github:acme/api", "not api, go with web", "zamiast acme/api uzyj acme/web", "use ops. actually no, skip it", "I would not use api, maybe ops". The person is told that the answer names a repository and also says no, that the workflow could not tell which repositories the no was about, and to name only the repositories to use the next time the question is asked. The ONE no that still decides is a reply that is nothing but a refusal, which declines everything the question listed (A7, A8, A9). Quoted lines and our own question come out before any of this is read, so a person quoting our comment and agreeing under it is not read as refusing (A11b) | Every defect this feature has had came from working out what a no attached to, and the ways people write a no do not end: "github:acme/infra, but please do not touch github:acme/billing" recorded billing as selected, "please do not touch github:acme/billing" alone selected billing, and a second thought that named nothing ("ops, no wait") was invisible to a reader that looked for clauses with repositories in them. A repeated question costs a round and explains itself; a decision recorded against somebody who wrote the opposite does not (rule 1). The reader is not on `main`, so production behaviour does not change (owner decision, round 4) | held |
| A11o | Writes prose whose negation is about something other than a repository ("none of the docs mention it") | Nothing is recorded, the question comes back, and they are told the ordinary thing: nothing in that answer named a repository this work should use. They are NOT told that their answer names a repository and also says no about it, which was two claims and both false, and which then taught them a rule they had not broken. A bare name counts as naming one only where a refusal stands beside it: a phrase that refuses on its own ("no, just ops") or a negation governing the name ("not api, ops", "nie ruszajcie web"). A negation that quantifies the words after it ("none of ...") is English, not a refusal | Rule 6 again, read on a sentence rather than on a route: a person cannot act on a description of their own words that is wrong, and the wrong one costs them the chance to write the right answer | held |
| A11j | Is asked the question repository discovery raises when it is not confident | The question lists the candidates nobody on this work has answered about and the record has not decided, and says that a proposed candidate the answer does not name is left out of this work from now on and no later run takes it on its own. It names no lever (rule 7), and it never opens the way the which-of-these question does, so the answer path never reads it as that question | The same binding as A11g, which this question used to leave unsaid (joint gate round 3, R6) | held |
| A11k | Answers a which-repository question, and the run reads the ticket again afterwards | The answer reaches the selection as an ANSWER and never as part of the ticket. No run appends it to the ticket it scans, and the path matcher never sees a person's reply | The reply used to be appended to the ticket as a comment, which put it in front of the path scanner: "not github:acme/billing" still names a path, so billing was matched, attached and written into the record as chosen by the ticket, which is the opposite of what the person wrote. The answer has one reader (round 4, B1) | held |
| A11l | Answers a question listing several repositories with "all", "both" or "wszystkie" | Every repository that question listed is recorded, as if they had been named one by one. Matched as the whole reply, so "all of the frontend ones" is still unreadable. A word that counts is read only against the count it means: "both", "oba", "obie", "obydwa" and "obydwie" on a question that listed two, "all three" and "wszystkie trzy" on a question that listed three. Said to a question listing four, such a word is unreadable and the question comes back, because it is about something other than the list in front of them, and what they are told names that: the word is a number of repositories and it is not the number the question listed, so answer "all" for every one it lists or name the ones to use. The sentence saying nothing in the answer named a repository was silent about the count, and the obvious second attempt was the same word | The question lists the choices, so the word has exactly one meaning, and costing a person a round for the least ambiguous reply there is buys nothing (round 4, m9). The counting words are the exception: "both" against a list of four is either a mistake or a reply to something else, and reading it as all four records four decisions off one wrong word (round 5, A1) | held |
| A11m | Clicks quote in Jira's rich editor and types their own reply INSIDE the quote block | Their words are dropped with the quote and they are told the answer named nothing, so the question comes back for a reply that named a repository plainly. Telling their line from ours inside a quote is what nothing here can do: our question is recognised and removed, and what is left is either somebody else's words or theirs, and every sentence this system writes about a repository it left out is built around a refusal, so reading a quoted line as theirs turns our own words into their no (A11b). Closing it needs a durable mark on each line this deployment posts, which is a change to what every comment looks like and not a reader fix. Owner: advisor, after the campaign | open, deliberate |
| A12 | Two people answer, and both comments arrive in one delivery | No entry is written, the trail records that an answer arrived, and the people are told | We cannot sign one decision with two names, and picking one of them is a fabrication | held |
| A13 | An automation rule comments under a person's name | Treated as that person, and it is a known limit | The payload says a person wrote it, and we have no evidence to the contrary | open, documented in code |
| A14 | Answers, and the run dies before it can act on it | The record already has the answer, because it is written when the answer ARRIVES and before the resume, and the NEXT run reads the record rather than the sentence | This is the defect the record exists to end: a run that dies seconds after an answer used to lose it | held |
| A14b | Answers after the question is no longer open, because the run was cancelled, the question expired, or a later round replaced it | Nothing is recorded, and the next run asks again. What the person is told comes from the run itself, which posts why it stopped or what it finished without, and the way back is the repository list rather than a reply | A comment can only be read as an answer while somebody is holding the question open. Threaded to one nobody is, it would be signed by a time window instead of by an answer, and that is the fabricated decision rule 1 puts below a repeated question | open, deliberate |
| A14c | Answers "none" while the question they were reading has been replaced by a differently shaped one | Their "none" is read against the NEW question's list. Recorded as a known limit rather than fixed: nothing threads a Jira comment to a question id, so the only evidence that the answer was about the earlier list is its timing, and deciding by a time window is the signature this feature refuses everywhere else (A14b). The window is narrowing from the other end: a cancelled question now announces itself on the ticket, so a person is less likely to be answering one that is gone. Owner: advisor, after the campaign | open, deliberate |
| A15 | Answers a question that listed no repository | Naming a path is recorded. Refusing records nothing, and the words offered back are paths, never "none of these", which would mean nothing | Never send a person to a vocabulary the question in front of them did not offer | held |
| A16 | Answers a question that asked them to narrow a set whose size they were told and whose names they were not shown | What they named is the whole answer for that subject, nothing is written about a name they never saw, and the question does not come back | They decided the scope. Twelve decisions about twelve repositories would be eleven fabrications | held |
| A16b | The same person would like to see the names before narrowing | They do not, this round. The question says how many there are and asks which are essential | The names a question prints decide what a later "none" binds, so printing them is a change to what answers MEAN, and it is not being made in the same change as the loop fix | deferred, deliberate |
| A16c | They narrowed to three, and by the next run all three are gone (renamed, archived, outside the workflow's pin) | A question, not a failed run, and it says first that what was chosen is not available to this run, naming up to five of them. Only then the plain question | Their three vanishing is new information and worth asking about, but a bare "which repository?" after somebody narrowed is the founding complaint wearing a different name. A run that dies over a repository renamed yesterday is worse than both | held |
| A17 | Splits one answer across a blank line | Read as one answer. A part that names the subject settles the whole | The parts are one person's sentence, and the naming half is the strongest evidence in it | held |
| A17b | Writes both halves of a refusal in ONE comment, across a line break or a comma (`no` + line break + `none of these`, `no, none of these`, `nope` + line break + `none`) | Read as the decline it is, on every channel: each phrase is a refusal, no phrase names anything else, and the one that names the subject settles the whole, exactly as it does across the blank line between two comments (A17). A bare `no` is untouched and still records nothing on the ticket (A8), because what lets a comment decide is a phrase saying what it refuses and never the number of phrases: `no` beside `nope` records nothing either, and the person is told what to write. A phrase that names a repository, or that carries any word the refusal list does not hold, leaves the whole reply unreadable (`no, use github:acme/api`, `no` + line break + `none of these, but check with the team first`). What each phrase may decline is its own question, and the question's shape decides it (A17c) | Production, AWP-221 on 2026-09-18: the reply was compared with the list whole, matched nothing, and the person was told to answer "none" the next time the question was asked, which is what they had just written. Saying it twice made an answer less readable than either half of it alone, and the reading turned on where they pressed enter | held |
| A17c | Refuses with words whose subject is ONE repository (`continue without it`, `no, continue without it`) | Read against what the question listed. Under a question about ONE repository, which is the question that offers those very words ("Reply \"none\" to continue without it"), it declines that repository and the record keeps the decision. Under a question listing several it is unreadable: nothing is recorded, the question comes back, and the person is told that nothing in their answer named a repository, so they may name the ones to use or answer "none", which does decline the whole list. The gate is the question's shape and never the channel, so the dashboard, MCP and the ticket read these words identically. A phrase that refuses the LIST (`none`, `none of these`, `żaden z nich`) is untouched and declines however many were listed, and a word carrying no subject at all (`no`) is not weighed against the list either: threaded to the question it takes that question's own subject, and which channels may thread it is A8 and A9. The RUN's own reading of an expansion answer is deliberately NOT gated: it ends that run's asking and writes nothing permanent, so a run carries on without the repositories it could not have while the record stays silent and the next run may ask again | Owner ruling, 2026-09-18: "no, continue without it" under a list of four is one person talking about one repository, and four permanent exclusions in their name is a decision nobody made. A repeated question is the cheap failure beside it (rule 1), and the same reasoning already gates a counting word on the count it means (A11l) | held |
| A17d | Refuses the whole list in the other English people write for a set: `neither`, `neither of them`, `neither of these`, `none of the above` | Each declines every repository the question listed, exactly as `none of these` does: the question closes and does not come back, a selection question writes no entry (A7), and a question raised because the catalog or the policy could not serve those repositories writes one `unavailable` or `excluded` entry each (A7b). Read the same on every channel, and on a ticket comment each of them decides rather than falling to A8, because each says what it refuses. `neither` is read against a list of any length, and is deliberately NOT gated on the count the way `both` is (A11l): a counting word that CHOOSES would attach repositories nobody named, while this one refuses, so the cost of reading it against four is a closed question rather than a fabricated selection. Added in English only: the tickets this reads are written in English, and a phrase nobody writes is a row nobody can check | Owner ruling, 2026-09-18. Each of these refuses a SET rather than a subject, so what they avoid is the repeat question rather than a fabricated decision: a person answering "none of the above" under four choices was read as naming nothing, so the identical question came back and they answered it twice. Reading them as a refusal of the list produces the outcome `none of these` already produces | held |
| A18 | Writes a colon in their answer on the dashboard or through MCP ("api: none", "github:acme/api: this is the one") | The colon is theirs and the whole reply is read. Nothing composes an author line on those channels, so the strip that takes "Ada: " off a ticket comment runs for the ticket alone, and for every reader of an answer there: the model that reads it, the record that decides from it, and the run's own refusal reader (A19d). Read as an author line, "api: none" became a bare "none" and declined every repository the question listed, and "github:acme/api: this is the one" lost the only repository it named and was answered with "nothing in that answer named a repository" | A decision fabricated out of punctuation is the failure this delivery puts above every other, and it happened before any reader saw the words. The tolerance stays for the channel that needs it: a composed answer really does open with a name and a colon | held |
| A19 | Writes anything at all, in words nobody on this team anticipated | **A MODEL READS IT, ONCE, WHERE THE ANSWER ARRIVES, INTO A CLOSED SET, AND THE READING IS STORED BESIDE THE WORDS.** The record and the resumed run both consume that one stored reading, so a "yes" can no longer mean a selection to one and noise to the other. This row governs every phrasing row above it: those are EXAMPLES the golden set holds us to, not a list the code matches, and the way to cover a new phrasing is a golden row, never a new phrase in the code. It replaces a phrase list that in one morning on production failed on `No. None of these.` for the full stop, on `Yes, please` for the comma, and on `none of the above` and `neither` for never having been thought of. Four things keep us safe given a model decides: the outcome is a closed set over keys WE supplied, so an invented or corrected key throws the whole reading away; not confident means ASK and never guess; a provider that is down falls back to the two unambiguous shapes only, a full path and the bare word "none", which is exactly the worst case this path had before a model was involved; and the person's words are untrusted DATA, said so in the prompt, in the wording the ticket-text reader already uses | held |
| A19b | Their words do not settle it ("whatever you think is best", "not the fixture one", anything the reading is not confident about) | Nothing is recorded, the run is NOT resumed, and the question stays open: the row stays pending, the ticket does not move, and no resume attempt is spent. They get a note in the channel they answered in saying what was read and asking for a yes or for the repository names, so their next reply is read against the same question. A name carrying a negation is never a selection of that name: under a list that is unclear, because four offered minus one refused is three the person never named, and under a question about one repository it is a decline of that one. An unclear reading costs one more question; a wrong one records a decision in the name of somebody who said the opposite | held |
| A19c | Names a repository the question did not offer ("api and web", on a question about api) | The offered one is recorded, the run resumes, and the person is TOLD that their answer also named web, that this question was only about api, that nothing was recorded about web, and where to add it. The allowlist stays narrow, because it is what an injected instruction runs into and the worst it may reach must stay another option the person was already being offered; the telling sits BESIDE the outcome, never inside it, so no reader switching on the outcome can act on it by accident, and the name is sanitised and bounded before it reaches anybody. Recording it was never the question: swallowing it was. A person who names two repositories because both are needed, and watches a run do half the job and finish green, is this delivery's founding complaint wearing a safety rule as a coat | held |
| A19d | Answers on a ticket, where every comment reaches us as "<their name>: <what they wrote>" | The reader is handed what they wrote, without the name the ticket already shows beside it, by the same rule the record reads by: one text, one reading, both of them the person's own words. The name comes off ONLY where the ticket channel composed one, per comment and never per paragraph, so a repository opening the second paragraph of one comment survives whole, and an answer typed on the dashboard or sent through MCP is read exactly as typed, colons included (A18) | Production, AWP-235 on 2026-09-18: handed "Filip Maszota: <their reply>", the reading paraphrased their reply back to them as one that "appears to reference Filip Maszota", a name they never typed. The dangerous half is quieter: a display name reads like a repository, so "Demo Team: fine by me" is one step from a selection of the demo repository written in the name of somebody who chose nothing, and the reading a model returns is what the run then acts on | held |

## B. Changing the record directly

Two things about how this is built, both read in the source rather than taken
from a report, because they are what keep the rows below from being three
separate answers to the same question. The authenticated route, the MCP tool,
the read, and the path a person's ANSWER takes when it arrives all go through one
service (`services/work-scope`), so there is one decision table and not one per
surface. And the write is a single
data-modifying statement that moves the version and the entries together, with
the version row locked before any entry is touched, because production runs on
a driver that cannot open an interactive transaction while the test driver can,
which is the classic way a concurrency bug passes every unit test and appears
only in production.

| # | The person | What must happen | Status |
|---|---|---|---|
| B1 | Reads the record for a subject that has one | The entries, with state, origin, who and when | held |
| B2 | Reads for a subject that has no record | An answer saying so, with an empty record, not an error | held |
| B3 | Selects a repository | Written as their decision. The next run uses it | held |
| B4 | Selects a repository that is not enabled | Refused, naming the repository, and the WHOLE edit is refused with nothing written. A person asking for three repositories of which one is not enabled gets none of them rather than two and a surprise | held |
| B4b | Selects any repository while the catalog has not been activated | Nothing is refused for that reason. While the catalog is the bridge every repository counts as enabled, which is how the deployment behaves today | held |
| B5 | Excludes a repository | Written, and it sticks: the next run will not put it back | held |
| B5b | Takes back an exclusion by selecting the repository again | The entry becomes their selection, carrying the rationale they give now, and the next run uses it. This is the way back from any exclusion, and it takes no new ticket | held |
| B6 | Removes an entry | The next run decides that repository again, except for what an answer on the trail still says. Removing an entry does not unmake an answer: if a which-of-these question named the repository, that question stays silenced and no guess takes the repository (C11), because the record can no longer tell an entry removed after an answer from an answer that wrote none. A selection brings it back, and so does a full path written in a ticket comment while the ticket names no more than three repositories. This reading was accepted on 2026-09-17 as it stands: telling the two apart would mean comparing the answer with the time the entry was removed, which the answered-set read does not do. The `remove` description on the MCP tool says so | held for the run; the tool's wording has no test of its own |
| B7 | Two people edit the same subject at once | The second is told it conflicted, and nothing is silently overwritten | held |
| B8 | Edits a subject that carries no record | Told that this subject carries no record, rather than one being created behind them | held |
| B9 | An agent edits through MCP | Today it is indistinguishable from a person downstream, so an agent can silence a question a person would have been asked | deferred, named gap |
| B10 | Edits the record while a run is in flight | The edit is written and the run in flight ignores it, and the next run starts from the changed list. A run freezes what it may reach at its start, so excluding something stops the NEXT run rather than one already working. The repository catalog obeys the same rule, and a record obeying a different one would be the surprise | held |

## C. What the run does with the record

| # | Situation | What must happen | Status |
|---|---|---|---|
| C1 | A fresh ticket with no record | Ask, and record the ask, so the answer is adjudicated | held |
| C2 | The record already carries a person's reachable selection | Do not ask again | held |
| C3 | The record carries a person's selection we cannot reach | Ask, and say why | held |
| C4 | Exclusions leave nothing to work on | Halt, say what was left out and why, and say the exclusion can be taken back and how | held |
| C5 | Exclusions leave nothing, but the run continues rather than halting | The question carries the same reasons. A bare "which repository?" after somebody's own exclusion is the founding complaint of this feature | held |
| C6 | More than eight repositories were left out | Say how many more and where to see them, rather than cutting the list in silence | held |
| C7 | A run suspended across the deploy of this feature | Takes the whole old path, decides nothing new, and does not diverge on replay | held |
| C8 | The subject is a webhook delivery with no ticket | No record, and no question | deferred |
| C9 | A schedule occurrence with no subject | No record | held |
| C10 | The run FINISHES, having left a repository out | The run's comment on the ticket lists it as left out with the same sentence a halt would have used, says once what the reader can do about it, and says how many more it did not list. This is the only surface that reaches a person on a run that did not halt. It includes a repository the ticket names that an answer on this work left unnamed while the which-of-these question stays silenced: each is keyed, with the way back under the case rule of C11 (joint gate round 3, R5). Every later run on the ticket says it again for as long as it holds, exactly as C11f does for three or fewer, because each comment reports its own run | held |
| C10b | That comment is too long for the ticket to take | The repositories section goes whole rather than in part, and the comment says by name which sections it dropped. A reader who is told there was more looks for it; a reader shown a trimmed list reads the last line as the last repository | held |
| C10c | The person does nothing about a repository the run left out, and more runs happen | Every later run repeats the identical list and the identical way back, word for word, for as long as the reason holds. That is deliberate: each comment reports its own run, and a run that quietly stopped mentioning a repository it did not open would read as a run that opened it. The cost is a long ticket carrying the same paragraph several times, and the only thing that ends it is the reason ending: the repository selected in the record, the exclusion taken back, the catalog enabling it, or the ticket's text dropping to three or fewer open repositories. Nothing here deduplicates against what an earlier comment said, because a run cannot know whether anybody read it (round 5, S5) | open, deliberate |
| C11 | A GUESS names a repository a which-of-these answer left unnamed: a remembered routing answer, the only-accessible shortcut, or a discovery proposal at any confidence | The run does not take it and does not ask, and no trail line is written for it: nothing was requested, so there is no request to refuse. It says that the repository was listed in a repository question already answered on this work and is not selected on it, in the prompt and, keyed, in the comment a finished run posts, without naming who answered or when (the run holds the answered repositories as keys alone). The way back goes to the person's channel only (rule 7), chosen by case: a selection in the record always, and a full path written in a ticket comment only when this run proves the next one would take it. That takes three facts at once, read from one value the run carries: the run scanned the ticket, it knows its own bot's account so a comment can be told from our question, and it can date the answer that named each repository the sentence is about (C11g); and then no more than three repositories may be open, counting the ones the ticket names as they stand when the sentence is written together with every repository the sentence offers, because a path written for them joins that count on the next run (C11c). The rule that binds the repository reads the same value, so the sentence cannot offer a comment the rule would ignore. The which-of-these question says beforehand that a repository it lists and the answer leaves out is left out from then on, and names no lever, because a question is copied into the agent's prompts and the memory file (rule 7); the lever rides the ticket comment beside it. Discovery left with nothing else fails the run with the same sentence and the way back, and where that way back is a path it says to write it in a comment on this ticket, never "in the ticket itself": an edit to the description is the text the question was already asked about and binds nothing (C11f) | held |
| C11b | The same run that asked is woken by the answer | It re-reads the answered repositories with the entries, so the answer binds the run that asked, not only the next one | held |
| C11c | The person wants a repository back after leaving it out of an answer, while more than three of the repositories the ticket names are still open | Nothing written on the ticket takes it while that holds, and what the run does next depends on what this work already carries. With no answer on it yet, the run asks which of them to start from rather than deriving any, a new comment included, so a path written in a comment becomes one of the choices. With an answer on it, the question is silenced for good and the run asks nothing: it takes none of them from the ticket's text and says so, naming every repository the ticket names and this run did not take, and naming separately each one somebody wrote the full path of AFTER answering, with its own reason and the way back (round 5, S1). Before that the run went quiet in exactly the case where somebody had done what we asked them to do. So the way back names only the record (the work scope API or `work_scope.edit`). What a person cannot do from Jira alone, for as long as that many stay open, is take the omission back: somebody with API or MCP access has to select the repository. It ends by itself as soon as three or fewer are open, because the text is read again from then on and a path written after the answer attaches (C11g). The run counts the open matches as they stand when it writes the sentence, together with the repositories it offers, and offers the comment only under the three conditions of C11; the answer-not-recorded comment, which has no run behind it, offers it only where it can prove it (A11f) and so only ever under-offers, and it promises no question either, because on a work that carries an answer none comes (round 5, S2). The direction accepted for later, not built here: a full repository path written in a comment AFTER the answer should be read however many repositories the ticket names, because that is a person naming it rather than a guess, and it may only be built if it costs no regression to the doors that work today. Owner: advisor, after the campaign | open, deliberate |
| C11d | An earlier guess recorded the repository as `selected` `inferred` before the question was asked, and the answer left it unnamed | Two things, and the second is what makes the rule work on records that already exist. The answer removes that entry, in the same single statement that records the answer (trail `entry_removed`, no new entry). And a `selected` `inferred` entry is read as no entry at all by the rule itself, so a record answered before this shipped, whose guess entry nobody ever removed, still refuses every guess. Only a guess is treated that way: a path somebody typed, a person's own selection, a trigger policy and a workflow-owned branch are entries and govern. The removal compares on origin, so a writer that changes that entry between the answer's read and its write makes the delete a no-op; the reading rule covers that case, which is why both exist | held |
| C11e | The agent asks mid run, through repository expansion, for a repository an answer left unnamed | Refused with its own reason, `unnamed_in_answer`, and never asked about: the run carries on. The model reads that the repository was listed in a repository question already answered on this work and is not selected on it, so it is not attached, which no exclusion sentence says. The trail gets one `request_refused` line, as every expansion refusal does. The person reads it too, in the comment the finished run posts: the repository, that the answer did not name it, and the way back under the case rule of C11. Today such a refusal still consumes one of the run's expansion rounds although no model work was needed; that is recorded as a gap and is not fixed here | held, except the round it consumes |
| C11f | The ticket's own description still names the repository, and an exclusion or the catalog drops the open matches to three or fewer, so the text branch runs again | Nothing is attached and nothing is written: that text is the very words the question was asked about, so reading it again is this system putting words in the person's mouth. The run says which repositories it left out and why, with the way back | held |
| C11g | A person writes the repository's full path in a ticket comment AFTER answering | Attached, and recorded with origin `ticket_text`: writing it out afterwards is a decision they took knowing what they left out. Only a comment written after the newest answer to a question that NAMED that repository counts, so an answer to a later question about another repository, or a question that only showed it as kept, does not turn an earlier comment into a pre-answer one; only from an author who is not this deployment's bot; and only a full `owner/repo` path, matched exactly as the ticket's own text is, against the repository's key rather than the listing, so a repository no longer listed is still recognised. The NEWEST such comment decides, and a comment that says no ("please do NOT touch github:acme/api") is not read as naming anything it mentions: the whole comment, because the reader cannot tell "don't touch api, but infra" from "don't touch api or infra". The run then says, on that repository's line, that the newest comment naming it also says no and was not read as naming it. A run that cannot date the comment, that does not know its bot's account, or that froze no answer instant for that repository, takes nothing from a comment for it, and does not offer the comment route for it either (C11) | held |
| C11h | A run suspended across the deploy that added the answered set wakes up and reaches the repository selection | It reads the record again rather than reading the missing field as "nothing was asked". The read happens inside the pre-sandbox step that already reads the store, so no step call is added and no suspended run's journal moves; a run that already passed that step replays its recorded result and is untouched | held |
| C11i | The agent asks mid run for a repository the record alone refuses, several times | Each refusal still consumes one of the run's expansion rounds, although the record answered it and no model work was needed, so a run can spend its expansion budget on questions nobody was asked. Fixing it needs a bound of its own for record-only refusals, or a model that keeps asking never stops researching | open |
| C11j | A run is already working in a repository whose entry the answer removed (C11d) | It carries on with that repository, and the comment the run posts does not say so. The person reads a pull request touching a repository they had just left out of their answer, with nothing anywhere explaining it | open |
| C11k | A person writes, after answering, the full path of a repository this run cannot open: not enabled on the Repositories page, no longer usable, or outside the workflow's pin | Nothing is attached and nothing is written, and the run says so, in the prompt and, keyed, in the comment a finished run posts: that the repository is not on the catalog this run may use, or is outside what the workflow may take. The person alone is also told what changes that: somebody can enable it, the catalog has to be able to serve it, or the pin has to change. On a catalog that was never activated a repository missing from the listing gets the sentence and no remedy, because nothing counts as disabled there. No trail line, because nothing about the work was decided. A comment that says no about the path reports nothing | held |
| C11l | Every repository the which-of-these question would offer is one this work already holds | No question is asked, and the run starts from what the record holds. There is nothing to choose between, so a question would only offer to drop repositories the answer cannot drop (A11g), and a person's time is the cost. Nothing was left out, so there is nothing to say either. One repository to choose beside held ones is still asked about, because the answer binds that one | held |
| C11m | The first run on a subject attaches a repository by a trigger policy or a workflow-owned branch in the same pass that raises the which-of-these question | That repository has no entry yet when the question is decided, so it is offered as a choice, and the question's sentence that an unnamed repository is left out is false for it: neither origin is bound by an answer (C12). Owner: advisor. Size: small, by reading the entries this run has written, as well as the frozen ones, when the question picks what is already held | open |
| C11n | An answer to the which-of-these question leaves an entry an earlier run derived from the ticket's text (`ticket_text`) for a repository it did not name | The entry stays and the repository stays attached; the question said beforehand that it is kept whatever the reply (A11g), so nobody is told a false thing. The alternative, that such an answer removes those entries as it removes a guess (C11d), is a decision about what an answer means and is the owner's to take | open, owner decision |
| C11o | An answer to a question that listed no repository at all records nothing | The person is told to write the full path in a comment, which the next run reads. The surface handling the answer cannot prove that on a ticket where a written path joins three open repositories and tips the run into asking instead, so this is the one remedy left that A11f's rule would not offer. Owner: advisor. Size: small, by giving that branch the same three-way value A11f uses | open |
| C11p | Repository discovery, unsure, proposes a repository somebody on this work already answered about, or one the record already decided | It is left off the question, which asks about the rest; a repository an answer only SHOWED as kept, or that a bare "no", a two-author delivery, a wordless answer or unreadable prose left unanswered, is not answered about and is still listed. Nothing left to ask means no question: every proposal already answered is the declined proposal, which fails the run with the way back where nothing else can start it, and a proposal the record decided is decided by the record (a person's selection is attached, an expired `unavailable` entry is taken again, A7b) | Nobody is asked again about what they already answered (joint gate round 3, R7) | held |
| C11q | The answer-not-recorded comment has to know which question it is about | It recognises the question about the ticket's text by the opening that the code asking it writes from the same constant, and one test fails if the two ever stop agreeing. A copy edit that broke the pair would fall to the sentence that under-promises. The durable carrier is the question's kind stored on the clarification row. Owner: advisor, owner decision (migration) | open |
| C11r | A comment on the ticket asks for a repository to be left alone ("Please do NOT touch github:acme/billing", "Use github:acme/api, do not touch billing"), by the rule in C11u2 | The comment is not read as naming anything, whole, so nothing in it is taken and nothing in it counts towards the open repositories the which-of-these question is raised on. Nothing is silenced for it: every repository such a comment names by its full path and the run did not take is reported on the run's own two channels, one sentence per repository, with the way back beside it in the person's channel alone, which is to write a comment naming only the repositories to work on. A REPOSITORY ALREADY IN THE RECORD IS NOT REMOVED BY A COMMENT: what an earlier run derived from the ticket's text survives, because a comment this run declined to read is not evidence that went away. The ticket's OWN words never get this reading, and are governed by C11f. A repository an earlier comment named and a later one takes back, by path or by bare name, is not taken ("use github:acme/ops", then "actually not ops"); on two comments carrying the same instant the refusal wins, so the reading does not depend on the order a provider happened to page them in | The reader cannot tell "not api, but infra" from "not api or infra", and attaching a repository somebody declined is the decision nobody made. The cost is a comment that named one repository to use beside one to leave alone, and it is paid out loud (owner decision, Q1, round 4 M4) | held |
| C11s | A person answers on the dashboard or over MCP, and the answer either records nothing or declines | They learn what their answer did in the channel they answered in. The answer's reply carries one sentence, optional and absent when the answer recorded exactly what it named: either why nothing was recorded, in the SAME words the ticket comment uses so somebody reading both meets one story, or, for a bare refusal, which repositories the question listed and were therefore declined, and the way back to any of them. The dashboard shows it under the answer and `runs.answer_clarification` returns it as `recordOutcome`, one field for both, with its description saying so. From these two channels the decline sentence goes to that channel alone and not to the ticket, where the run itself already reports what it started without. The answer reaches the run either way | The ticket comment reaches the ticket's readers, and somebody answering from a screen or a client may never open it: they saw "answered" and met the same question on the next run (owner decision, Q2). A decline is worse still, because it is the most consequential thing one word can do here, one permanent entry per repository in that person's name, and the rule lived only in a tool description no human reads (round 5, A4) | held |
| C11s2 | A person declines in a ticket COMMENT ("none of these"), which leaves every repository the question listed out of this work for good | The ticket gets a comment naming them and the way back, in the same words the other two channels carry. The decline sentence existed and went back as the answer call's reply, which on this path nobody ever sees: the answer WAS the comment, and there is no screen behind it, so the most consequential thing three words can do here was told to nobody. Posted once per decision: a redelivery of the identical answer is the same decision arriving again | A decline is told in the channel that took it (C11s), and for this channel that is the ticket | held |
| C11s4 | Answers by NAMING a repository, on a question that listed more | The reply they get back names the repositories the question listed and their answer left out, says they are left out of this work with no later run taking them on its own, and gives the way back in the words a decline is given it. It goes to the channel that took the answer and not to the ticket, where the run itself already lists what it started without (C10). Until this existed a bare "no" got the whole sentence and the person who answered carefully got nothing, although both bind the same repositories the same way (C11) | The quieter half of the same rule as C11s: the most consequential thing an answer does here is what it leaves out, and it was told to nobody | held |
| C11s3 | That sentence is on the dashboard, and the person reloads the page | It is gone. `recordOutcome` comes back with the answer call and lives in the screen's own state, so a refresh, a second person opening the same run, or coming back tomorrow shows the answer with nothing beside it. Keeping it would mean storing it on the clarification row, which is a migration and a contract change rather than a screen fix. The ticket comment still carries it for an answer that recorded nothing, and for a decline typed on the dashboard nothing else does. Owner: advisor, after the campaign | open |
| C11t | A person selected a repository that is not enabled, and discovery proposes it | Discovery asks about it as a repository nobody enabled, although the person already chose it; the candidate filter of C11p covers the low-confidence question, not the question about a repository the catalog refused. Owner: advisor, after the campaign. Size: small, a `selected` entry read beside `excluded` and `unavailable` where discovery decides what it already knows | open |
| C11u | The ticket's own description names a repository only where it asks for it to be left alone ("Do NOT touch github:acme/api, it is frozen") | The description is read one PHRASE at a time, and a repository whose every mention in it sits in a phrase that keeps this system out is not taken from the ticket. The run reports it by name, with the reason and the way back beside it, rather than starting in it. A phrase that names it plainly elsewhere still takes it, so "Work in github:acme/web. Do not touch github:acme/api." takes web and leaves api. This REVERSES the earlier ruling that the description never gets the negation reading (round 5, S3): a comment is a reply and could be about anything, but a description is the statement of the work, and a run that starts in the one repository the ticket told it to leave alone is the worst outcome this feature has. Phrase by phrase rather than whole, unlike a comment (C11r), because a description is written as a document and reading it whole would drop every repository named in a ticket that says no about anything | Under-deriving costs a question; taking a frozen repository silently costs a person the repository | held |
| C11u2 | What counts as keeping this system out, in the description and in a comment alike | A phrasing a person writes about a REPOSITORY, sharing a phrase with the path it is about. Two kinds. The ones that are about scope and cannot mean anything else count wherever they stand in the phrase: "do not touch", "do not use", "do not include", "leave it out", "keep it out", "out of scope", "not in scope", "nie ruszaj", "nie dotykaj", "nie używaj". The ones that take an object count only where the object is the repository, spelled out or called one: "skip github:acme/api", "exclude that repo", "ignore the repository", "pomiń github:acme/api", "github:acme/api is frozen", "repozytorium jest zamrożone", "bez github:acme/web", and a refusal standing directly in front of it ("not github:acme/api", "actually not ops"). "Skip the migration in github:acme/api", "Ignore the failing test in github:acme/web" and "The schema is frozen in github:acme/api" therefore TAKE the repository: each tells us what not to do inside one we are being asked to work in. A bare "not", "never", "forget", "instead" or "rather" is not a phrasing at all, and neither is one about an ACTION ("do not deploy", "nie wdrażaj"). A phrase ends at a comma, a semicolon, a colon, a dash, a sentence end or a line break, so "Fix the login bug in github:acme/api, but do not deploy yet" takes api and "Do NOT touch github:acme/api, it is frozen" leaves it. A list header carries to the items under it ("Do not touch:" and a bullet list), and stops at a blank line, the next header, or the first line that is neither | The round before read any negation word as a refusal of the repositories named beside it, which is the most ordinary ticket anybody writes: "Fix the login bug in github:acme/api, but do not deploy yet" dropped api, "Don't forget to update github:acme/docs" dropped docs, and each person was told by name that their own words had said no about the repository they had just asked for. The reverse failure, a repository taken silently, is why the phrasings that really do keep us out still bind | held |
| C11u3 | Any of that text is quoted, in the description or in a comment ("> Do NOT touch github:acme/api") | A quoted line is nobody's own words here and is read by neither reader: the repository is not taken from it and nothing is said about it. The two readers used to disagree, the one deciding whether the words said no dropping quoted lines and the one finding paths reading them raw, so a quoted refusal in a description was disarmed on one side and matched on the other and the run cloned the frozen repository. A person quoting OUR sentence about a repository this work left out and agreeing underneath does not thereby name it either: the key in it is ours | One text and one segmenter, or the two rules drift apart again. The cost is the text a person types INSIDE a quote block, which is under-derived (A11m) | held |
| C11u4 | A path and the words that keep us out of it are separated by a colon or another phrase break ("github:acme/api: repozytorium jest zamrożone", or a "Repos:" list whose item reads "github:acme/api: frozen") | The repository is TAKEN. The two land in different phrases, so the phrase naming the path carries nothing that keeps us out and the phrase that does names no path. It costs the thing this rule exists to prevent: the run works in a repository the ticket called frozen, and says nothing about it, because on its reading nobody asked it not to. It is this way because the phrase is the unit that makes the ordinary ticket work: "Fix the login bug in github:acme/api, but do not deploy yet" keeps api only because the comma ends the phrase, and widening the unit back to the sentence brings that defect back. The direction if we take it, and it is narrow enough to be safe: a phrase holding nothing but a path binds to the phrase that follows it, so the path inherits what is said about it and nothing else changes. Owner: advisor, after the campaign | open |
| C11v | A person names, in the ticket's text, a repository this deployment ENABLES and the providers offer nothing checkoutable for: archived, or listed with no default branch | Nothing is attached and nothing is written, and the run says once, keyed, that the repository is enabled here and this run could not check it out, the provider having listed it as archived or offered no default branch for it. It is never said as "not on the catalog this run may use", which is false about an enabled row and contradicts the way back standing in the same comment. The sentence describes what the PROVIDER GAVE US rather than what the repository IS, because a token without code access on a GitLab project yields no default branch for a perfectly healthy repository, and telling that person their repository is empty sends them hunting a fault that is ours. It is a durable fact rather than a flaky one: a provider that failed or timed out contributes no repositories at all and keeps its own separate sentence. A repository that is DISABLED and unusable is reported unusable, because enabling it would cost the person a round and end at the same answer; one that is unusable and outside the pin is reported unusable, because widening the pin would not give them their repository. Nothing is said about an unusable repository nobody named, and nothing about one a question on this work already named, because there the person's own answer is their latest word | held |
| C11w | The ticket's own text names a repository, and a person has excluded it on this work, so the record is why this run is not taking it | The run says which repository, who excluded it and on what day, ahead of the question it asks or beside the work it does, and the way back rides the ticket comment alone, never the questions array, which is copied into the agent's prompts and into the memory file under "Human decisions". Bounded per REPOSITORY, not by whether the run found anything else: a run that attached another repository, opened a pull request covering half the ticket and finished green is exactly the case this exists for, and bounding it on an empty selection kept it silent there. Bounded by the ticket, so a record holding old exclusions the ticket does not mention adds nothing. Said ONCE: the vague line about deriving nothing from the ticket's text is filtered by what has already been said by name, and with nothing left to add it says nothing at all. Before this, the person read a bare "Which repository or repositories should this ticket inspect or modify?" on a ticket whose first sentence named one, with their own earlier decision invisible, because the exclusion is dropped by `decidableKeys` before any signal proposes it and the refusal that names the author and the date therefore never happens | held |
| C11x | A person names, in the ticket's text, a full provider-scoped path on a provider this workflow's pin EXCLUDES | The run says once that the repository is outside what this workflow may take, answered from the key alone. The pin stops that provider being queried at all, so the repository is in no listing and therefore in none of the three sets built from one: before this the run worked in what it did find, opened a pull request covering half the ticket and finished green with nothing said anywhere, which is the founding complaint arriving through the very reason added to end it. Answered from the key rather than by widening the listing, because `gitlab:acme/ops` names its provider whether or not we ever call GitLab, and querying a provider the pin excludes is a network call to prove what the person already wrote. A path that names no provider stays silent, because which provider it is on would be a guess | held |
| C12 | A workflow-owned branch, or a full path typed in the ticket, names a repository an answer left unnamed | Taken. Neither is a guess: one is a pull request that must not be stranded, the other is a person naming it | held |
| C12b | A definition pin or a trigger policy names it | Taken, for the same reason, and untouched by this rule | open, no test of its own |
| C13 | The run carries no record at all, and a person's answer is the only evidence it has | It uses the answer. Only a `ticket:`, `pr:` or resolved `webhook:` subject keeps a record, so a schedule occurrence, a subject-less delivery and a plan approval never carry one, and this state is permanent for them rather than a leftover the restructure drains away. Such a run reads the answer itself: it attaches what the answer names, up to the three repositories that are the ambiguity limit everywhere else, and where a named path is not in the catalog it asks about that path by name. A reply that says no about anything is not used, and the run asks rather than attaching the repository the person refused. It posts no sentence about an answer it could not use, because there is nothing on this path that reports to a person; the question is the report | An answer naming two repositories used to land here only because the answer was also appended to the ticket the path scanner reads. That append is gone (A11k), so honouring one repository and dropping the rest in silence would be a regression on the one path that cannot explain itself (round 5, S4 and A5) | held |
| C14 | The run fails because the agent still needs a repository after expansion closed | The sentence names the way back that works for THAT repository, the way the question already does: one this deployment does not enable is sent to the Repositories page and a new run, and one it could use but did not attach is sent to this work's repository list through the work scope API or the `work_scope.edit` tool. The refusal riding the repeated request is what tells them apart, because a question the work scope raised leaves nothing in the expansion loop's own state, and reading only that state sent a repository nobody can attach to "attach it and start a new run" | Production, 2026-09-18: the run said "enable it on the Repositories page" in the question and "attach it" in the failure two comments later, about one repository, and the later sentence is the one the person read at the end. Two ways out of one wall, and the one they act on last is the one that cannot work (rule 6) | held |

## D. What the agent is allowed to see

| # | Channel | Carries | Status |
|---|---|---|---|
| D1 | The research, implementation and review prompts | The questions and the answers, including what was left out and why | held |
| D2 | The run's memory file, under "Human decisions" and "do not edit or remove" | The questions and the answers. Never a sentence about reversing somebody's decision | held |
| D3 | The ticket's comments, rendered verbatim into the research prompt on the NEXT run | Whatever anybody wrote on the ticket, our own comments included | held |
| D4 | The reversal sentence | May appear in D3, because that is ticket history a person could have written. Never in D1 or D2, because those are instructions the system signs. The way back from an omission in an answer (C11) obeys the same line | held |

The distinction in D4 is the one that cost us a wrong ruling: the halt text was
believed to be the person's channel, and it is not, because the same string is
prefixed onto the first question. The failure path already posts the sentence to
the ticket, so withholding it from a person on any other path protects nothing
and only tells that person less.

## What is deliberately not decided here

- **The screen.** The record is editable through the API and MCP. A panel is
  stage 7.
- **An agent's edit versus a person's.** Closing this needs a durable marker in
  the contract and in the database, which is not a change to make days before a
  merge. Until then, an agent holding the edit tool can write what reads as a
  person's decision.
- **A webhook subject with no ticket.** It carries no record today.

## How this document is kept honest

Every row is a claim about behaviour, so every row that says **held** has a test
that fails when that row stops being true. A row that cannot be expressed as a
test is a row that is not understood yet, and it stays **open** until it is.
Marking a row held from a report, rather than from a failing test, is the one
thing that makes this document worse than not having it.
