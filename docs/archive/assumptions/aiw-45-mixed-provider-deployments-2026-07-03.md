| Assumption | Load-bearing? | Weight | Verdict | Evidence (citation) | Gate status |
|---|---|---|---|---|---|
| Work should target an isolated AIW-45 feature branch, not the main workspace branch. | yes | critical | TRUE | `git rev-parse --abbrev-ref HEAD` in `.worktrees/aiw-45-multi-repo-support` -> `codex/aiw-45-multi-repo-support`; `git worktree list` -> branch checked out at `/Users/karol/Desktop/ai-workflow/.worktrees/aiw-45-multi-repo-support`. | pass |
| Existing uncommitted AIW-45 edits must be preserved before rebasing. | yes | high | TRUE | `git status --short --branch` -> branch ahead 3, behind 2, with modified AIW-45 files; `git diff --stat` -> 19 files changed. | pass |
| AIW-45 already has repository-level provider metadata and workflow-owned branch records to extend. | yes | critical | TRUE | `git show 00c40fb:apps/worker/src/adapters/vcs/repository-directory.ts` -> `RepositoryMetadata.provider`, `SelectedRepository.provider`; `git show 00c40fb:apps/worker/src/db/schema.ts` -> `workflow_owned_branches` primary key includes `ticketKey`, `provider`, `repoPath`. | pass |
| The current AIW-45 execution path still relies on one global VCS config in multi-repo code. | yes | critical | TRUE | `rg "getVcsConfig\\(|getVcsToken\\(|buildVcsUrls\\(" apps/worker/src` -> hits in `repo-selection.ts`, `repository-prs.ts`, `agent.ts`, `manager.ts`, and `poll-agent.ts`. | pass |
| The codebase already has provider repository discovery hooks for both GitHub and GitLab. | yes | high | TRUE | `git show 00c40fb:apps/worker/src/adapters/vcs/repository-directory.ts` -> GitHub uses `apps.listReposAccessibleToInstallation`; GitLab uses `/api/v4/projects?membership=true&simple=true`. | pass |
