// apps/dashboard/app/(cockpit)/presandbox/page.tsx — Pre-sandbox ("/presandbox")
import { PreSandboxScreen } from "@/components/cockpit/screens/presandbox";

export default function PreSandboxPage() {
  return <PreSandboxScreen />;
}
